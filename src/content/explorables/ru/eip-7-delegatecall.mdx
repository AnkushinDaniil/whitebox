---
title: "EIP-7: DELEGATECALL — занимаем код, сохраняем свой контекст"
dek: "Контракты хотели переиспользовать логику общей библиотеки, не копируя её байткод в каждый из них. Но обычный CALL выполняет эту логику в собственном хранилище библиотеки — бесполезно для библиотеки, которая должна работать с вашими данными. DELEGATECALL выполняет заимствованный код в контексте вызывающего — и тем самым тихо изобрёл библиотеки, прокси и обновляемые контракты."
summary: "Пошаговая деривация EIP-7 (DELEGATECALL), выпущенного в хардфорке Homestead (2016). Почему контрактам нужно было переиспользовать общий код без дублирования байткода; почему обычный CALL выполняет код вызываемого в хранилище вызываемого (с вызывающим в роли msg.sender), из-за чего общая библиотека работает с данными не того аккаунта; как CALLCODE выполнял код в хранилище вызывающего, но затирал msg.sender; и как DELEGATECALL чинит оба недостатка — выполняя код вызываемого целиком в контексте вызывающего (хранилище и баланс вызывающего, исходные msg.sender/msg.value сохранены). Заканчивается тем, что это открывает: библиотеки Solidity, паттерн прокси, обновляемые контракты и прямую линию к EIP-7702."
topic: EIPs
order: 16
spine: true
prerequisites: ["EVM и модель вызовов", "хранилище и контекст"]
updated: 2026-06-24
readingMinutes: 11
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

Мы входим в Часть III с самого начала: **Homestead, март 2016 года**, первый запланированный апгрейд Ethereum. Он выпустил маленький опкод с огромными последствиями. К этому моменту (из [главы про EVM](/the-evm)) вы знаете, что контракт — это код плюс хранилище, и что один контракт может **вызвать** другой. Но вызов поднимает вопрос, на который самый ранний EVM ответил плохо: если я хочу переиспользовать код *другого* контракта — общую библиотеку проверенной логики — *чьи данные затрагивает этот код?* Ответите неправильно — и общие библиотеки невозможны; ответите правильно — и откроете прокси и обновляемые контракты. **EIP-7** — это правильный ответ, `DELEGATECALL`. Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Переиспользование без копирования" },
  { id: "step-2", label: "CALL выполняется не там" },
  { id: "step-3", label: "Занимаем код, сохраняем контекст" },
  { id: "step-4", label: "Прокси и апгрейды" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Переиспользование без копирования">

## Проблема: общая логика без копипаста

Деплой контракта стоит газ, пропорциональный размеру его кода, а одни и те же рутины — безопасная математика, переводы токенов, контроль доступа — пишутся снова и снова. Копипастить байткод библиотеки в каждый контракт расточительно и неподдерживаемо: исправьте баг — и придётся передеплоить *всех*. Очевидное желание — единственный, проаудированный контракт-**библиотека**, задеплоенный один раз, чей код могут занимать многие контракты.

<Scene name="dc-copy" lang="ru" caption="Каждый контракт, несущий свою копию одной и той же логики перевода — это чистое дублирование: дорого деплоить и невозможно пропатчить в одном месте. Желание: задеплоить логику один раз как общую библиотеку и дать многим контрактам её выполнять. Но это поднимает настоящий вопрос — какое хранилище затрагивает общий код?" />

<Callout type="warning" title="Что ломается">
«Просто вызовите библиотеку» — кажется, что это решает проблему. Но вызов другого контракта выполняет *его* код в *его собственном* мире. Для библиотеки, вся работа которой — оперировать *вашими* балансами, это ровно наоборот. Давайте посмотрим, как это ломается.
</Callout>

**→ Шаг 2: смотрим, как обычный CALL попадает не в то хранилище.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="CALL выполняется не там">

## Почему обычный CALL не годится для библиотеки

Когда ваш контракт делает обычный `CALL` к библиотеке, EVM переключается в <KeyTerm term="контекст исполнения" def="Аккаунт, чьё хранилище, баланс и адрес читает и пишет выполняющийся код, плюс msg.sender/msg.value. Обычный CALL переключает контекст на вызываемого; код выполняется «как бы от имени» вызываемого." /> **библиотеки**. Код библиотеки выполняется, но каждый `SSTORE`, который он делает, пишет в хранилище *библиотеки*, `address(this)` — это *библиотека*, а `msg.sender` — это *ваш* контракт. Так что метод библиотеки вроде `balances[msg.sender] += amount` исправно обновляет отображение балансов **библиотеки** — не ваше. Ваше хранилище вообще не затрагивается.

<Scene name="dc-call" lang="ru" caption="Обычный CALL выполняет код библиотеки в контексте библиотеки: запись попадает в хранилище библиотеки, а для библиотеки msg.sender — это ваш контракт. Собственное хранилище вашего контракта никогда не записывается. Библиотека, изменяющая данные вызывающего, таким способом невозможна." />

Код абсолютно исправен — он просто выполнился в мире не того аккаунта. На самом деле нам нужна обратная схема: *код* библиотеки, но *наш* контекст.

<Callout type="warning" title="Что ломается">
Нам нужно выполнить код вызываемого, но чтобы каждое чтение/запись хранилища, баланс и адрес были *нашими*. Более ранняя попытка, `CALLCODE`, действительно выполняла код в хранилище вызывающего — но она перезаписывала `msg.sender` вызывающим, ломая любую логику, проверяющую, кто вызвал изначально. Можем ли мы получить и заимствование кода, *и* сохранить свою идентичность нетронутой?
</Callout>

**→ Шаг 3: DELEGATECALL.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Занимаем код, сохраняем контекст">

## DELEGATECALL: код вызываемого, всё остальное — вызывающего

<KeyTerm term="DELEGATECALL" def="Вызов EVM (EIP-7), который загружает код вызываемого и выполняет его целиком в контексте вызывающего: хранилище и баланс вызывающего, адрес вызывающего как this, и — в отличие от более старого CALLCODE — исходные msg.sender и msg.value сохранены. «Выполнить этот код так, будто он мой»." /> — это в точности та схема, которая нам была нужна. Он загружает **код** цели, но выполняет его в **вашем** контексте: чтение и запись попадают в *ваше* хранилище, `address(this)` — это *вы*, баланс — *ваш*. И он чинит недостаток `CALLCODE` — он **сохраняет исходные `msg.sender` и `msg.value`**, так что заимствованный код видит того же вызывающего и то же значение, с которыми был вызван ваш контракт. Это в точности *«выполнить код этого контракта так, будто он часть моего»*.

<Scene name="dc-delegate" lang="ru" caption="DELEGATECALL загружает код библиотеки, но выполняет его в вашем контексте: запись попадает в ВАШЕ хранилище, баланс и адрес — ваши, а исходные msg.sender/msg.value сохранены. Библиотека фактически становится дополнительными методами, прикрученными к вашему контракту." />

<Formula
  lang="ru"
  expr="DELEGATECALL:  code = callee  ·  context = caller"
  terms={[
    { t: "code = callee", note: "выполняемые инструкции — это байткод целевой библиотеки" },
    { t: "context = caller", note: "хранилище, баланс, address(this) и исходные msg.sender/value — вызывающего" },
  ]}
  caption="Разделите то, что связывает вызов: берите код оттуда, оставляйте контекст здесь. В этом одном разделении — вся идея."
/>

<SpecExcerpt
  lang="ru"
  source="EIP-7: DELEGATECALL"
  href="https://eips.ethereum.org/EIPS/eip-7"
  ref="семантика"
>
Добавляет новый опкод `DELEGATECALL` (0xf4) с той же раскладкой стека, что у `CALLCODE`,
но без аргумента value. Он выполняет код целевого адреса в контексте вызывающего
контракта — то же хранилище, тот же `CALLER` (`msg.sender`) и `CALLVALUE` (`msg.value`),
что и в текущем окружении. Он ведёт себя так, как будто вызываемый код — часть
вызывающего.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Ничего не ломается — но открывается дверь. Если контракт может выполнить код *любого* другого контракта как свой собственный, его поведение больше не зафиксировано при деплое. Что можно построить, разделив *хранилище* контракта и его *логику*?
</Callout>

**→ Шаг 4: отделяем состояние от логики — и обновляем.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Прокси и апгрейды">

## Награда: библиотеки, прокси, обновляемые контракты

Как только код и контекст можно разделить, паттерны появляются сами. Вызовы `library` в Solidity компилируются в `DELEGATECALL`, так что общая логика живёт в блокчейне в одном экземпляре. Более того, можно разбить систему на тонкий <KeyTerm term="прокси" def="Минимальный контракт, который хранит всё состояние и при каждом вызове делает DELEGATECALL к отдельному контракту-реализации за логикой. Поскольку состояние живёт в прокси, замена адреса реализации обновляет поведение без перемещения данных — а адрес, который знают пользователи, никогда не меняется." />, хранящий всё **состояние**, и отдельную **реализацию**, хранящую **логику**. Каждый вызов к прокси делегируется реализации, которая работает с хранилищем прокси. Чтобы обновиться, вы просто указываете прокси на новую реализацию — данные остаются на месте, а адрес, которым все пользуются, никогда не меняется.

<Scene name="dc-proxy" lang="ru" caption="Прокси хранит всё состояние и делает DELEGATECALL своей логики к контракту-реализации. Замените реализацию с v1 на v2 — и поведение обновится, а состояние (и адрес, который знают пользователи) останутся ровно на месте. Так работают обновляемые контракты." />

<Callout type="insight" title="И это и есть DELEGATECALL">
Контрактам был нужен общий код без копипаста → обычный **CALL** выполняет библиотеку в контексте *библиотеки*, так что записи попадают не в то хранилище → **CALLCODE** выполнял её в хранилище вызывающего, но затирал `msg.sender` → **DELEGATECALL** выполняет *код* вызываемого в *полном контексте* вызывающего, сохраняя исходные `msg.sender`/`msg.value`. Разделение кода и контекста — крошечное изменение с огромным радиусом воздействия: библиотеки Solidity, паттерн прокси и каждый промышленный обновляемый контракт происходят от этого одного опкода. И идея возвращается на новом витке — [EIP-7702](/eip-7702-set-eoa-code) даёт обычным EOA дескриптор, заставляющий их выполнять код контракта в *своём собственном* контексте — то же прозрение DELEGATECALL, применённое к аккаунтам вместо контрактов. Опкод 2016 года всё ещё формирует дорожную карту 2025-го.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-7: DELEGATECALL", note: "Спецификация — опкод и правила его контекста", href: "https://eips.ethereum.org/EIPS/eip-7" },
    { kind: "post", label: "Глава 2 — The EVM", note: "Модель вызовов и контекст хранилища, на которых это строится", href: "/the-evm" },
    { kind: "post", label: "EIP-7702, derived", note: "Та же идея, принесённая обычным EOA", href: "/eip-7702-set-eoa-code" },
    { kind: "eip", label: "EIP-1167: Minimal proxy", note: "Крошечный контракт-клон, построенный на DELEGATECALL", href: "https://eips.ethereum.org/EIPS/eip-1167" },
    { kind: "code", label: "OpenZeppelin — proxy patterns", note: "Промышленные реализации обновляемых контрактов", href: "https://docs.openzeppelin.com/contracts/5.x/api/proxy" },
  ]}
/>

</DepthLayer>
