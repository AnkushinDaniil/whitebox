---
title: "EIP-3541: резервируем байт для будущего"
dek: "Как оставить место для добавления совершенно нового формата контрактов позже, если каждый возможный первый байт кода уже является валидным опкодом? Никак — если только не сделать один байт запретным намеренно. EIP-3541 запрещает деплой нового кода, начинающегося с 0xEF, превращая этот байт в зарезервированное пространство, на котором строятся более поздние апгрейды (EOF и делегирующий дескриптор EIP-7702)."
summary: "Пошаговая деривация EIP-3541, вышедшего в London (август 2021). Почему для внедрения нового ончейн-формата кода нужен маркер, с которого не может начинаться устаревший байткод, — и почему такого маркера не существовало, ведь каждое значение байта 0x00–0xff уже декодируется как какой-то опкод EVM; как EIP-3541 решает это волевым решением, отклоняя деплой любого нового контракта, чей код начинается с 0xEF (уже существующие контракты с 0xEF получают освобождение задним числом); и что открыл этот один зарезервированный байт: префикс контейнера EVM Object Format (EOF) 0xEF00 и делегирующий дескриптор EIP-7702 0xef0100 — оба полагаются на то, что обычный код контракта не может начинаться с 0xEF."
topic: EIPs
order: 22
spine: true
prerequisites: ["код контракта и опкоды", "деплой контрактов"]
updated: 2026-06-24
readingMinutes: 8
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**London, август 2021 года.** Наряду с рынком комиссий и исправлением возвратов, London принёс изменение, которое *само по себе ничего не делает* — оно просто заставляет определённый деплой провалиться. Звучит бессмысленно, пока не увидишь, для чего это нужно. Ethereum хотел оставить себе пространство для внедрения совершенно нового **формата кода контрактов** в будущем форке — структурированного, версионированного, проверяемого заранее — вместо сегодняшнего байткода, который принимается как есть. Но добавление нового формата требует способа отличить его от старого, и это оказывается на удивление сложным. **EIP-3541** — небольшой, дальновидный ход, который делает это возможным. Выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Ни один байт не свободен" },
  { id: "step-2", label: "Резервируем 0xEF" },
  { id: "step-3", label: "Что это открыло" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Ни один байт не свободен">

## Проблема: каждый байт уже опкод

Код контракта — это просто последовательность байтов, и EVM читает их как <KeyTerm term="опкоды" def="Однобайтовые инструкции EVM. Код контракта — это сырая байтовая строка, интерпретируемая по одному опкоду за раз — 0x60 это PUSH1, 0x01 это ADD и так далее. Нет ни заголовка, ни тега типа; первый байт — это просто первая инструкция." />. Чтобы внедрить новый формат, хотелось бы иметь **магический префикс** — первый байт, говорящий: *«не читай меня как обычный байткод, я — новая вещь»*. Проблема в том, что *каждое* значение байта от `0x00` до `0xff` уже что-то значит как опкод (или является определённой недействительной инструкцией), так что нет значения, которое можно объявить маркером, не будучи уже валидным кодом. Новому формату нечем заявить о себе.

<Scene name="ef-nomark" lang="ru" caption="Код контракта — это сырая байтовая строка, читаемая опкод за опкодом — 60 это PUSH1, 01 это ADD. Каждый из 256 возможных первых байтов уже декодируется как некая инструкция, так что ни один не свободен, чтобы переназначить его в маркер «это новый формат»." />

<Callout type="warning" title="Что ломается">
Нельзя задним числом навесить маркер на байт, который уже используется легитимным кодом, — это сломало бы существующие контракты или создало неоднозначность. Чтобы получить чистый маркер, нужно гарантировать, что *ни один* код не начинается с некоторого выбранного байта. Но сегодня ничто не мешает контракту начинаться с любого байта. Если только не вмешается протокол.
</Callout>

**→ Шаг 2: намеренно сделать один байт запретным.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Резервируем 0xEF">

## EIP-3541: отклоняем новый код, начинающийся с 0xEF

Решение — это правило, а не механизм: начиная с London, **любая попытка задеплоить новый код контракта, чей первый байт — `0xEF`, проваливается** — создание откатывается, и контракт не создаётся. Этот единственный запрет превращает `0xEF` в <KeyTerm term="зарезервированный префикс" def="Значение байта, с которого протокол запрещает начинать новый код контракта, чтобы будущие апгрейды могли безопасно придать ему особое значение. EIP-3541 зарезервировал таким образом 0xEF." /> — байт, гарантированно никогда не начинающий обычный байткод, а значит свободный для будущего формата. (`0xEF` выбрали, потому что это был неопределённый опкод, напоминающий «EVM Format»; горстка уже задеплоенных контрактов, начинающихся с `0xEF`, получает освобождение задним числом, поскольку правило блокирует только *новые* деплои.)

<Scene name="ef-reserve" lang="ru" caption="Начиная с London, деплой кода, начинающегося с 0xEF, отклоняется полностью, а любой другой первый байт деплоится как раньше. Это делает 0xEF зарезервированным префиксом — гарантированно свободным, — которому будущий форк может придать значение без всякого риска столкновения с настоящим байткодом." />

<SpecExcerpt lang="ru"
  source="EIP-3541: Reject new contract code starting with 0xEF"
  href="https://eips.ethereum.org/EIPS/eip-3541"
  ref="правило"
>
После форка London создание нового контракта (через `CREATE`, `CREATE2` или создающую
транзакцию) проваливается, если первый байт возвращаемого/деплоимого кода — `0xEF`. Код,
уже задеплоенный до форка, не затрагивается. Байт зарезервирован, чтобы будущие форматы
(например, EVM Object Format) могли использовать его как магический префикс.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Само по себе ничего не меняется — просто нельзя задеплоить контракт, начинающийся с одного конкретного байта. Весь смысл — в том, что эта резервация *позволяет* позже.
</Callout>

**→ Шаг 3: обналичить резервацию.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Что это открыло">

## Итог: магический префикс для новых форматов

Когда `0xEF` гарантированно свободен, будущий форк может безопасно объявить: *«код, начинающийся с `0xEF…`, — не устаревший байткод, интерпретируй его особо»*, с нулевым риском неверно прочитать настоящий контракт. На этом напрямую строятся две вещи. <KeyTerm term="EOF" def="EVM Object Format — структурированный, версионированный контейнер контракта (заголовок, секции кода/данных, проверяемый при деплое), начинающийся с магических байтов 0xEF00. Он полагается на то, что 3541 зарезервировал 0xEF." /> использует префикс `0xEF00` для своего версионированного контейнера. А ещё — связь, замыкающая петлю в этой книге, — делегирующий дескриптор [EIP-7702](/eip-7702-set-eoa-code) — это `0xef0100 ‖ address`: когда 7702 присваивает коду EOA это значение, он использует зарезервированный префикс `0xEF` именно для того, чтобы это значение нельзя было спутать с обычным байткодом.

<Scene name="ef-unlock" lang="ru" caption="Поскольку 0xEF зарезервирован, форк может однозначно определить, что значит 0xEF…: контейнер EOF (0xEF00 + версионированный код) и делегирующий дескриптор EIP-7702 (0xef0100 + адрес делегата) — оба живут за этим одним зарезервированным байтом." />

<Callout type="insight" title="Вот и весь EIP-3541">
Добавление нового формата кода требует маркера, с которого устаревший код никогда не может начинаться → но каждый байт уже валидный опкод, так что ни один не свободен → так что **резервируем `0xEF` волевым решением**: отклоняем любой новый контракт, чей код начинается с него, делая байт гарантированно свободным → и будущие форки тратят эту резервацию, придавая `0xEF…` особое значение без риска столкновения. Это изменение, вся ценность которого отложена: единственный запрещённый байт в 2021 году стал префиксом для **EVM Object Format** и для делегирующего дескриптора **EIP-7702** `0xef0100` — механизма, позволяющего EOA выполнять код контракта. Иногда самое важное, что делает апгрейд, — это тихо держать дверь открытой.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-3541: Reject code starting with 0xEF", note: "Спецификация — правило резервации", href: "https://eips.ethereum.org/EIPS/eip-3541" },
    { kind: "post", label: "EIP-7702, derived", note: "Его дескриптор 0xef0100 полагается на эту резервацию", href: "/eip-7702-set-eoa-code" },
    { kind: "eip", label: "EIP-3540: EOF v1", note: "Версионированный контейнер, занимающий 0xEF00", href: "https://eips.ethereum.org/EIPS/eip-3540" },
    { kind: "post", label: "Chapter 2 — The EVM", note: "Почему код — это просто строка опкодов", href: "/the-evm" },
    { kind: "tool", label: "evm.codes", note: "Таблица опкодов — убедитесь, что свободного байта нет", href: "https://www.evm.codes/" },
  ]}
/>

</DepthLayer>
