---
title: "EIP-4337: абстракция аккаунта без изменения протокола"
dek: "EOA негибки: один ключ, газ только в ETH, одно действие за раз. Абстракция аккаунта делает аккаунты программируемыми — и EIP-4337 добивается этого без единого изменения консенсуса, используя транзакцию более высокого уровня, отдельный мемпул, бандлеров и синглтон EntryPoint."
summary: "Пошаговая деривация ERC/EIP-4337 — почему EOA ограничены, почему нативная абстракция аккаунта потребовала бы хардфорка, и как 4337 получает смарт-аккаунты (мультиподпись, passkeys, восстановление, батчинг, спонсируемый или токен-газ) целиком в прикладном слое: UserOperation в отдельном мемпуле, бандлеры, упаковывающие их в одну транзакцию, синглтон EntryPoint, который валидирует и исполняет каждую, и пеймастеры, оплачивающие газ."
topic: EIPs
order: 6
spine: true
prerequisites: ["EOA против контрактов", "мемпул"]
updated: 2026-06-23
readingMinutes: 14
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

Есть два вида аккаунтов Ethereum: **контракт** (программируемый, но не может *начать* ничего сам — выполняется только при вызове) и **аккаунт, управляемый извне** (EOA — может начинать транзакции, но негибкий). Всё, что вы делаете, начинается с EOA, а EOA застрял с одним подписывающим ключом, газом только в ETH и одним действием за транзакцию. Люди хотели аккаунты, которые *программируемы* — мультиподпись, passkeys, социальное восстановление, батчинг, возможность дать кому-то другому оплатить ваш газ. Это **абстракция аккаунта**. Очевидный способ её получить — изменить протокол так, чтобы аккаунты могли быть контрактами со своими правилами, — хардфорк, годы согласований. **EIP-4337** задаёт более острый вопрос: можно ли получить *всё это* при **нулевых** изменениях консенсуса? Давайте выведем, как.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Негибкий EOA" },
  { id: "step-2", label: "UserOperation" },
  { id: "step-3", label: "Бандлеры и EntryPoint" },
  { id: "step-4", label: "Пеймастеры" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Негибкий EOA">

## Проблема: EOA — это смирительная рубашка

Каждая транзакция берёт начало от EOA, а EOA может делать ровно то, что жёстко закодировано в протоколе. Его правило валидности фиксировано: *одна валидная подпись secp256k1 над транзакцией, правильный nonce и достаточно ETH.* Это единственное правило исключает почти всё, чего хотят люди.

<Scene name="aa-eoa" lang="ru" caption="Правила EOA фиксированы: один ключ ECDSA, газ в ETH, одно действие за транзакцию. Мультиподпись, passkeys, оплата газа в USDC, батчинг, социальное восстановление — каждое желание отскакивает." />

Один ключ означает, что его потеря — это потеря всего: ни мультиподписи, ни аппаратных passkeys, ни социального восстановления. Газ только в ETH означает, что вы обязаны заранее держать ETH, чтобы вообще что-то сделать: ни спонсора, ни оплаты в USDC. Одно действие за транзакцию означает, что `approve`, а затем `swap` — это две подписи, две комиссии. Аккаунт-*контракт* мог бы закодировать любое правило, какое захотите (<KeyTerm term="абстракция аккаунта (account abstraction)" def="Возможность определять валидность аккаунта его собственным кодом — любая схема подписи, любая политика газа, батчинг — вместо единого фиксированного протокольного правила ECDSA-и-ETH." />) — но контракт не может *инициировать* транзакцию; он выполняется только тогда, когда его кто-то вызывает.

<Callout type="warning" title="Что ломается">
Мы хотим, чтобы аккаунт был контрактом с кастомной валидацией. Но контракты не могут начинать транзакции, а сделать это возможным *нативно* — глубокое изменение консенсуса, хардфорк. Вопрос превращается в: можем ли мы обойти протокол целиком и получить это без единого изменения правил консенсуса хоть одного клиента?
</Callout>

**→ Шаг 2: изобретаем транзакцию, которая живёт на уровень выше.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="UserOperation">

## Транзакция уровнем выше: UserOperation

Не трогаем то, что такое *транзакция*. Добавляем новый объект уровнем выше: <KeyTerm term="UserOperation" def="Структура 4337, описывающая, что смарт-аккаунт хочет сделать — аккаунт, calldata, лимиты газа и подпись, которую сам аккаунт проверит любой удобной ему схемой. Это не транзакция Ethereum; она путешествует в отдельном мемпуле." />. Она описывает, что смарт-аккаунт хочет сделать — адрес аккаунта, `callData` (действие, которое может быть *батчем*), лимиты газа и `signature`, которую **сам аккаунт** проверит как ему угодно. Пользователь подписывает её любой схемой, которую использует его аккаунт, — ключом ECDSA, passkey телефона, мультиподписью 2 из 3, — и рассылает в **отдельный, альтернативный мемпул** только для UserOperation, отличный от обычного мемпула транзакций.

<Scene name="aa-userop" lang="ru" caption="Смарт-аккаунт не может сам отправить транзакцию, поэтому вы подписываете UserOperation — объект более высокого уровня — и рассылаете его в собственный мемпул, отдельный от обычного мемпула транзакций. Никаких изменений консенсуса: просто новая форма данных и новая gossip-сеть." />

Здесь в консенсусе ничего не изменилось. UserOperation — не транзакция, которую понимает протокол, — это сообщение прикладного уровня в новой одноранговой сети. Что поднимает очевидную проблему:

<Callout type="warning" title="Что ломается">
Цепочка никогда не слышала о UserOperation. Что-то должно реально превратить её в настоящую ончейн-транзакцию — и *оплатить* её.
</Callout>

**→ Шаг 3: пусть кто-то упакует её и запустит.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Бандлеры и EntryPoint">

## Бандлеры их упаковывают; EntryPoint их запускает

Встречайте <KeyTerm term="бандлер (bundler)" def="Обычный узел (EOA), который следит за мемпулом UserOperation, упаковывает батч UserOp в одну обычную транзакцию к EntryPoint, авансирует газ L1 и получает возмещение из депозитов аккаунтов." />: обычный узел — запустить его может кто угодно, — который следит за мемпулом UserOp, выбирает батч и упаковывает их в **одну** обычную транзакцию Ethereum. Он отправляет эту транзакцию синглтон-контракту **<KeyTerm term="EntryPoint" def="Единственный аудированный контракт (один на сеть), через который 4337 маршрутизирует всё. Его handleOps проходит циклом по каждой UserOp, сначала валидируя, затем исполняя, и аккаунты/пеймастеры доверяют только ему." />**, который проходит циклом по каждой UserOp в двух строго разделённых фазах:

1. **Валидация** — EntryPoint вызывает `validateUserOp(...)` аккаунта, который выполняет *любую кастомную логику, определённую аккаунтом* (проверить подпись passkey, порог мультиподписи, сессионный ключ) и должен согласиться платить.
2. **Исполнение** — EntryPoint вызывает аккаунт с `callData` из UserOp — реальным действием, которое может объединять несколько вызовов в один.

<Scene name="aa-bundler" lang="ru" caption="Бандлер (EOA) собирает UserOp и упаковывает их в единую транзакцию к синглтон-EntryPoint, который валидирует (запуская собственную логику каждого аккаунта), а затем исполняет каждую — авансируя газ и получая возмещение от аккаунтов." />

Бандлер авансирует газ L1 и получает возмещение от EntryPoint из предварительно пополненного депозита каждого аккаунта. Так почему же его нельзя обмануть, заставив включить UserOp, которая не заплатит? Он сначала **симулирует** валидацию каждой UserOp офчейн, а правила запрещают валидации читать хранилище *других* аккаунтов — поэтому UserOp не может изменить собственную валидность на основании состояния, которое может сдвинуть другая транзакция, и симуляция остаётся правдивой вплоть до включения.

<SpecExcerpt lang="ru"
  source="ERC-4337: Account Abstraction Using Alt Mempool"
  href="https://eips.ethereum.org/EIPS/eip-4337"
  ref="UserOperation"
>
UserOperation несёт `sender, nonce, callData, callGasLimit, verificationGasLimit,
preVerificationGas, maxFeePerGas, maxPriorityFeePerGas, paymasterAndData, signature`
(плюс опциональные `factory`/`initCode` для развёртывания аккаунта при первом использовании). `handleOps([...], beneficiary)` у EntryPoint прогоняет цикл валидации, затем цикл исполнения, и платит
`beneficiary` (бандлеру) из депозита каждого отправителя.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Это уже даёт смарт-аккаунты — но аккаунту всё ещё нужен ETH на депозите, чтобы платить. Мы обещали спонсируемый газ и оплату в других токенах. Откуда берётся *это*?
</Callout>

**→ Шаг 4: пусть третья сторона оплатит счёт.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Пеймастеры">

## Пеймастеры: платит кто-то другой, или платите любым токеном

Добавляем одну опциональную сторону: контракт-**пеймастер**. UserOperation может указать пеймастера, который соглашается покрыть её газ. Это открывает две вещи, которых люди действительно хотели. Dapp может **спонсировать** ваши транзакции — вы не держите вообще *никакого* ETH, и всё просто работает. Или пеймастер может позволить вам **платить газ в ERC-20**, например в USDC: он забирает у вас токен и платит газ в ETH от вашего имени. EntryPoint сводит счета в конце и возмещает бандлеру из депозита пеймастера (или аккаунта).

<Scene name="aa-paymaster" lang="ru" caption="Пеймастер может полностью спонсировать ваш газ или принять оплату любым токеном и покрыть ETH сам. EntryPoint сводит счета аккаунтов и возмещает бандлеру — так что вы можете совершать транзакции без единого ETH." />

<Callout type="insight" title="И это EIP-4337">
EOA негибки → делаем аккаунт *контрактом* с кастомной валидацией → но контракты не могут отправить сами себя → значит **UserOperation** живёт уровнем выше, в собственном мемпуле → **бандлеры** упаковывают батч в одну транзакцию к синглтон-**EntryPoint**, который валидирует (любой логикой, какую пожелает аккаунт), а затем исполняет (даже батчем) каждую → **пеймастеры** платят газ или принимают токены. Каждый элемент вынужден недостатком предыдущего, и всё это работает в **прикладном слое** — контракты плюс офчейн-инфраструктура, *ноль* изменений консенсуса. Цена — дополнительная сантехника: отдельный мемпул, бандлеры, EntryPoint. Награда — мультиподпись, passkeys, социальное восстановление, батчинг и спонсируемый или деноминированный в токене газ — и всё это без единой просьбы к каждой команде клиента о хардфорке. (А естественный следующий вопрос — можем ли мы дать обычным EOA кусочек этого *без* дополнительного слоя? — это именно то, на что отвечает EIP-7702.)
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "ERC-4337: Account Abstraction", note: "Спецификация — UserOp, EntryPoint, пеймастеры", href: "https://eips.ethereum.org/EIPS/eip-4337" },
    { kind: "code", label: "eth-infinitism — account-abstraction", note: "Референсные EntryPoint и бандлер", href: "https://github.com/eth-infinitism/account-abstraction" },
    { kind: "post", label: "Ключи, подписи и адреса", note: "Фиксированное правило ECDSA, от которого уходит 4337", href: "/you-could-have-invented-ethereum#step-2" },
    { kind: "eip", label: "EIP-7702 (следующий)", note: "Приносит кусочек этого обычным EOA", href: "https://eips.ethereum.org/EIPS/eip-7702" },
    { kind: "tool", label: "erc4337.io", note: "Экосистема аккаунтов, бандлеров и пеймастеров", href: "https://www.erc4337.io/" },
  ]}
/>

</DepthLayer>
