---
title: "EIP-2718: конверт, позволивший Ethereum добавлять типы транзакций"
dek: "У Ethereum был ровно один формат транзакции — RLP-список. Каждая новая функция (списки доступа, рынок комиссий 1559, блобы, set-code) требовала новой формы, и декодерам приходилось угадывать, какую из них они видят. EIP-2718 добавляет в начало каждой транзакции один байт типа, превращая один жёсткий формат в расширяемое, версионированное семейство."
summary: "Пошаговая деривация EIP-2718 (типизированный конверт транзакции) — почему один RLP-формат превращал каждую новую функцию транзакции в игру в угадайку, несовместимую с обратной совместимостью, и как один ведущий байт типа это исправляет: типизированная транзакция — это TransactionType ‖ TransactionPayload, байт типа 0x00–0x7f никогда не может столкнуться с legacy RLP-списком (который начинается с ≥ 0xc0), так что одного взгляда на первый байт достаточно, чтобы отличить legacy от типизированной, — а каждый последующий EIP (2930, 1559, 4844, 7702) просто занимает следующий номер."
topic: EIPs
order: 8
spine: true
prerequisites: ["RLP-кодирование", "что такое транзакция"]
updated: 2026-06-23
readingMinutes: 11
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

На протяжении многих лет у Ethereum был ровно **один** вид транзакции: единый <KeyTerm term="RLP" def="Recursive Length Prefix — каноническая сериализация Ethereum. Она кодирует вложенные списки и байтовые строки; транзакция была RLP-кодировкой списка из девяти полей [nonce, gasPrice, gasLimit, to, value, data, v, r, s]." />-кодированный список из девяти полей. Это было нормально, пока люди не захотели *изменить* то, чем может быть транзакция — добавить списки доступа, заменить `gasPrice` на пару базовая-комиссия/чаевые из [1559](/eip-1559-fee-market), нести обязательства по [блобам](/eip-4844-blobs), прикреплять авторизацию [set-code](/eip-7702-set-eoa-code). Каждая новая функция требует новой формы на проводе, и в момент, когда форм становится больше одной, каждый декодер сталкивается с вопросом, на который он никогда не был рассчитан отвечать: *какой это вид транзакции?* **EIP-2718** — это небольшое, непритязательное исправление, сделавшее возможными все знаменитые EIP после него. Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Один формат, много желаний" },
  { id: "step-2", label: "Добавляем байт типа" },
  { id: "step-3", label: "Никогда не сталкиваться с legacy" },
  { id: "step-4", label: "Версионированное семейство" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Один формат, много желаний">

## Проблема: одна форма, которую все обязаны угадывать

Исходная транзакция *и есть* `rlp([nonce, gasPrice, gasLimit, to, value, data, v, r, s])` — ничего больше. Нет ни поля версии, ни тега, ни метки. Чтобы добавить функцию, нужно менять этот список: добавить `accessList`, подставить новые поля комиссии. Но теперь по сети летают два разных вида транзакций, и единственный способ их различить — **принюхаться** к RLP: посчитать поля, изучить структуру и *угадать*. Догадки хрупки: два формата могут выглядеть тревожно похоже, а каждому кошельку, узлу и эксплореру нужна одна и та же хрупкая эвристика, которую придётся поддерживать в синхроне вечно.

<Scene name="tx-onefmt" lang="ru" caption="Одна форма RLP для каждой транзакции, без тега версии. Добавьте функцию — и байты изменятся, так что каждому декодеру приходится вынюхивать структуру и угадывать, какой вид у него в руках — хрупко, и с каждым новым форматом становится хуже." />

Нет чистого способа сказать *«это новый вид транзакции, декодируй его по этим правилам»*. У формата нет места объявить о себе.

<Callout type="warning" title="Что ломается">
Нам нужно добавлять форматы транзакций со временем, не ломая существующие — и не заставляя декодеры гадать. Какая наименьшая вещь, которую можно добавить, заставит транзакцию *объявлять* свой формат заранее?
</Callout>

**→ Шаг 2: приклеим ярлык на конверт.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Добавляем байт типа">

## Оборачиваем в конверт: один байт типа, затем полезная нагрузка

Хватит пытаться закодировать формат *внутри* данных. Вместо этого приклеим ярлык снаружи. Определим <KeyTerm term="типизированная транзакция" def="Конверт EIP-2718: транзакция, закодированная как один байт типа, за которым следует непрозрачная, специфичная для типа полезная нагрузка — TransactionType ‖ TransactionPayload. Байт типа называет формат; полезную нагрузку умеют читать только правила этого формата." /> ровно как две части, соединённые вместе: `TransactionType ‖ TransactionPayload`. Первый байт — небольшое число, называющее формат; всё после него — непрозрачная полезная нагрузка, которую умеют читать только *правила именно этого типа*. Декодер читает один байт, находит подходящий набор правил и декодирует остальное с полной уверенностью — никакого принюхивания, никаких догадок.

<Scene name="tx-typebyte" lang="ru" caption="Типизированная транзакция — это байт типа, за которым следует специфичная для типа полезная нагрузка. Ведущий байт сообщает каждому декодеру, какой именно набор правил применить — прочитайте один байт, затем декодируйте остальное с уверенностью." />

Это просто **версионированный конверт**. Байт типа — селектор версии/формата; смысл полезной нагрузки определяется для каждого типа отдельно. Добавление нового формата больше не означает изменение существующей формы — оно означает занять новый номер и описать, как выглядит его полезная нагрузка.

<Callout type="warning" title="Что ломается">
Изящно — но сеть *полна* старых, нетипизированных транзакций, и у них вообще нет байта типа. Если новая транзакция теперь начинается с байта вроде `0x02`, как декодеру не спутать старую транзакцию с типизированной (или наоборот)?
</Callout>

**→ Шаг 3: сделаем так, чтобы два вида нельзя было перепутать.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Никогда не сталкиваться с legacy">

## Обратная совместимость бесплатно: выбираем байты, которых RLP не производит

Вот в чём изящество. **Legacy**-транзакция — это RLP-*список*, а RLP кодирует список с первым байтом в диапазоне `0xc0`–`0xff`. Типизированная транзакция определена так, чтобы использовать байт типа в диапазоне `0x00`–`0x7f`. Эти диапазоны **не могут пересекаться** — RLP-список никогда не начинается ниже `0xc0`, а байт типа никогда его не достигает. Так что декодеру достаточно взглянуть на самый первый байт: если он `≥ 0xc0` — это legacy RLP-транзакция; если `≤ 0x7f` — типизированная, и значение байта говорит, какого типа. Одно сравнение, ноль неоднозначности, оба вида сосуществуют вечно.

<Scene name="tx-disambig" lang="ru" caption="Одного первого байта достаточно для различения. Legacy RLP-список всегда начинается с ≥ 0xc0; типизированным транзакциям выделен диапазон 0x00–0x7f, который RLP никогда не может произвести. Поэтому старые и новые транзакции сосуществуют без столкновений и без принюхивания." />

Значения `0xc0` и выше зарезервированы именно для того, чтобы legacy-транзакции оставались действительными, а `0xff` оставлен в стороне (зарезервирован). Дизайн не *добавил* метку к старым транзакциям — он выбрал диапазон новой метки так, чтобы старая кодировка уже сама себя различала.

<SpecExcerpt lang="ru"
  source="EIP-2718: Typed Transaction Envelope"
  href="https://eips.ethereum.org/EIPS/eip-2718"
  ref="конверт и диапазоны"
>
Транзакция — это либо `TransactionType ‖ TransactionPayload`, либо `LegacyTransactionRlp`.
`TransactionType` — число в диапазоне `0 ≤ type ≤ 0x7f`; `TransactionPayload` — непрозрачные
байты, чей смысл определяется типом. Поскольку legacy-транзакция — это RLP-список (первый
байт `≥ 0xc0`), эти два вида однозначны. Квитанция типизированной транзакции аналогично —
`TransactionType ‖ ReceiptPayload`.
</SpecExcerpt>

<Formula
  lang="ru"
  expr="typed_tx = TransactionType ‖ TransactionPayload"
  terms={[
    { t: "TransactionType", note: "один байт, 0x00–0x7f — называет формат" },
    { t: "TransactionPayload", note: "непрозрачные байты, которые умеет декодировать только этот тип" },
  ]}
  caption="Один ведущий байт превращает «формат транзакции» в расширяемое, версионированное семейство."
/>

<Callout type="insight" title="Подписи тоже переезжают в конверт">
Legacy-транзакция подписывалась поверх своих сырых полей; типизированная транзакция подписывается поверх `keccak256(TransactionType ‖ rlp(payload-without-signature))`. Байт типа — часть того, что подписывается, поэтому транзакцию одного типа никогда нельзя повторно провести как транзакцию другого — конверт аутентифицирован, а не просто описателен.
</Callout>

**→ Шаг 4: смотрим, как растёт семейство.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Версионированное семейство">

## Выигрыш: каждый последующий EIP просто занимает байт

Как только конверт существует, добавление формата транзакции становится рутиной. [EIP-2930](https://eips.ethereum.org/EIPS/eip-2930) занял `0x01` для списков доступа; [EIP-1559](/eip-1559-fee-market) занял `0x02` для рынка базовой комиссии/чаевых; [EIP-4844](/eip-4844-blobs) занял `0x03` для транзакций с блобами; [EIP-7702](/eip-7702-set-eoa-code) занял `0x04` для авторизаций set-code. Каждый — просто число плюс описание полезной нагрузки. И, что важно, старый клиент, не распознающий байт типа, **аккуратно отклоняет** транзакцию вместо того, чтобы неверно её прочитать — неизвестное значит неизвестное, а не «угадай».

<Scene name="tx-family" lang="ru" caption="С версионированным конвертом каждая новая функция просто занимает следующий байт типа и описывает свою полезную нагрузку — 0x01 списки доступа, 0x02 рынок комиссий 1559, 0x03 блобы, 0x04 set-code. Старые клиенты аккуратно отклоняют неизвестный тип вместо того, чтобы неверно его декодировать." />

<Callout type="insight" title="Вот и весь EIP-2718">
Одна форма RLP превращала каждую новую функцию в несовместимую с обратной совместимостью игру в угадайку → так оберните каждую транзакцию в конверт: один **байт типа**, затем непрозрачная **полезная нагрузка** → выберите диапазон типа `0x00–0x7f`, с которым RLP-список (первый байт `≥ 0xc0`) никогда не может столкнуться, так что legacy и типизированные сосуществуют благодаря проверке одного байта → и теперь каждый последующий EIP просто занимает следующий номер. EIP-2718 сам по себе не добавляет *ни одного* нового типа транзакции; это чистая инфраструктура — формат для форматов. Именно поэтому 2930, 1559, 4844 и 7702 смогли появиться, ни разу не заставив сеть заново учиться отличать транзакции друг от друга. Цена была почти нулевой: один байт. Выигрыш — десятилетие безболезненной расширяемости.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-2718: Typed Transaction Envelope", note: "Спецификация — конверт и диапазоны типов", href: "https://eips.ethereum.org/EIPS/eip-2718" },
    { kind: "eip", label: "EIP-2930: Optional access lists", note: "Тип 0x01 — первый пользователь конверта", href: "https://eips.ethereum.org/EIPS/eip-2930" },
    { kind: "post", label: "EIP-1559, выведенный", note: "Тип 0x02 — рынок комиссий, который это открыло", href: "/eip-1559-fee-market" },
    { kind: "post", label: "EIP-4844, выведенный", note: "Тип 0x03 — транзакции с блобами", href: "/eip-4844-blobs" },
    { kind: "tool", label: "RLP, visually", note: "Как первый байт legacy-списка попадает в ≥ 0xc0", href: "https://ethereum.org/en/developers/docs/data-structures-and-encoding/rlp/" },
  ]}
/>

</DepthLayer>
