---
title: "EIP-140: REVERT — дёшево проваливаться и объяснять почему"
dek: "До Byzantium контракт, столкнувшийся с плохим условием, имел только один грубый вариант — throw: он откатывал работу, но при этом сжигал весь газ вызывающего до последней капли и не возвращал никакого объяснения. REVERT сохраняет откат, возвращает газ и позволяет контракту вернуть ошибку. Каждый require(\"reason\"), кастомная ошибка и try/catch, которые вы когда-либо писали, — потомки этого опкода."
summary: "Пошаговая деривация EIP-140 (REVERT), выпущенного в форке Byzantium (октябрь 2017). Почему прерывание вызова контракта раньше означало throw — что откатывало состояние, но потребляло весь оставшийся газ и не возвращало данных, наказывая законные проваленные условия и не давая способа их объяснить; как опкод REVERT (0xfd) одновременно исправляет все три проблемы, атомарно откатывая изменения состояния текущего фрейма, возвращая неиспользованный газ и возвращая блок данных с ошибкой; и как эта единственная инструкция лежит в основе require с текстом причины, кастомных ошибок и try/catch, ведь revert вложенного вызова возвращается к вызывающему как перехватываемый провал с указанной причиной. Отмечены и другие крупные изменения Byzantium — STATICCALL и прекомпайлы спаривания alt_bn128, которые сделали возможной верификацию zk-доказательств on-chain."
topic: EIPs
order: 18
spine: true
prerequisites: ["EVM, газ и модель вызовов", "изменения состояния"]
updated: 2026-06-24
readingMinutes: 10
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

**Byzantium, октябрь 2017.** Крупный форк — он принёс криптографические прекомпайлы, сделавшие возможными доказательства с нулевым разглашением on-chain, и тип вызова, который не может трогать состояние. Но изменение, которое каждый Solidity-разработчик ощущает ежедневно, куда скромнее: новый способ *проваливаться*. До этого момента контракт, обнаруживший плохое условие — списание сверх баланса, неудачную проверку прав, — мог только `throw`, а throw был актом выжженной земли: он откатывал работу, но при этом сжигал каждую единицу газа, оставшуюся у вызывающего, и не возвращал ни байта объяснения. **EIP-140** вводит `REVERT`, и вместе с ним провал становится дешёвым, точным и *читаемым*. Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Провал был выжженной землёй" },
  { id: "step-2", label: "Откат, возврат газа, объяснение" },
  { id: "step-3", label: "Перехватывается вызывающим" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Провал был выжженной землёй">

## Проблема: прерывание стоило всего и не говорило ничего

Вызов контракта либо завершается успешно и фиксирует изменения состояния, либо прерывается — и тогда их нужно откатить: вы никогда не хотите *наполовину применённого* перевода. До Byzantium единственным способом прерваться было <KeyTerm term="throw" def="Способ прерывания до Byzantium: выполнение невалидной инструкции (или throw в Solidity), которое откатывало изменения состояния фрейма, но потребляло весь оставшийся газ и не возвращало данных." /> — обычно натыкаясь на невалидный опкод. Это действительно откатывало состояние, и это правильно. Но было два жестоких побочных эффекта: потреблялся **весь** оставшийся у вызывающего газ, и не возвращалось **никаких данных**. Так что вполне разумный `require(balance >= amount)`, который просто не выполнился, сжигал весь газовый бюджет пользователя *и* оставлял его смотреть на голое «транзакция не выполнена» без единой причины.

<Scene name="rev-throw" lang="ru" caption="До REVERT неудачная проверка вызывала throw: состояние фрейма корректно откатывалось, но весь оставшийся газ сжигался, и данные об ошибке не возвращались. Законный, ожидаемый провал стоил пользователю всего газового бюджета и ничего не сообщал о том, что пошло не так." />

<Callout type="warning" title="Что ломается">
Откатывать состояние при провале — правильно. Сжигать весь газ за *ожидаемое* условие — карательно, а отсутствие причины делает контракты неотлаживаемыми и несоставимыми. Нам нужен атомарный откат — *без* потраченного впустую газа и *со* способом сказать, что произошло.
</Callout>

**→ Шаг 2: сохраняем откат, убираем наказание, добавляем причину.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Откат, возврат газа, объяснение">

## REVERT: три исправления в одном опкоде

<KeyTerm term="REVERT" def="Опкод Byzantium 0xfd. Останавливает текущий вызов, атомарно откатывает все изменения состояния этого фрейма, возвращает вызывающему неиспользованный газ и возвращает блок данных (offset, length) — им пользуются, чтобы передать причину ошибки." /> (опкод `0xfd`) делает ровно то, что нам нужно, — сразу три вещи. Он **откатывает** все изменения состояния, сделанные в текущем фрейме вызова, — атомарно, будто фрейм вообще не выполнялся. Он **возвращает** оставшийся газ вместо того, чтобы его сжигать, поэтому ожидаемый провал стоит только газа, реально потраченного до этого момента. И он **возвращает данные** — произвольный блок `(offset, length)` из памяти, — в котором и уезжает обратно строка ошибки или структурированный код ошибки.

<Scene name="rev-revert" lang="ru" caption="REVERT атомарно делает три вещи: ① откатывает каждое изменение состояния в текущем фрейме, ② возвращает вызывающему неиспользованный газ вместо того, чтобы его сжигать, и ③ возвращает блок данных — так контракт может вернуть причину вроде «InsufficientBalance» вместо того, чтобы провалиться молча." />

<Formula
  lang="ru"
  expr="REVERT  =  undo(state)  +  refund(gas)  +  return(data)"
  terms={[
    { t: "undo(state)", note: "каждый SSTORE, изменение баланса и лог в этом фрейме откатывается — атомарно" },
    { t: "refund(gas)", note: "неиспользованный газ возвращается вызывающему; теряется только уже потраченный газ" },
    { t: "return(data)", note: "блок памяти всплывает наверх — это строка ошибки или данные кастомной ошибки" },
  ]}
  caption="Старый throw делал только первое, да ещё и по максимальной цене. REVERT сохраняет атомарный откат, но добавляет возврат газа и причину."
/>

<SpecExcerpt
  lang="ru"
  source="EIP-140: REVERT instruction"
  href="https://eips.ethereum.org/EIPS/eip-140"
  ref="семантика"
>
Добавляет опкод `REVERT` (`0xfd`), принимающий два аргумента стека `(offset, length)`. Он
останавливает выполнение и откатывает изменения состояния, сделанные текущим вызовом, но —
в отличие от невалидной инструкции — **не** потребляет весь оставшийся газ: неиспользованный
газ возвращается вызывающему. Диапазон памяти `(offset, length)` возвращается как return-данные
вызова.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Ничего не ломается — но возвращённая причина полезна, только если что-то способно её *получить*. Что происходит с этой ошибкой, когда откатывающийся код сам был вызван другим контрактом?
</Callout>

**→ Шаг 3: пусть вызывающий её перехватит.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Перехватывается вызывающим">

## Выигрыш: провал, который вызывающий может перехватить и прочитать

Поскольку `REVERT` возвращает *данные* и *не* уничтожает транзакцию целиком, вложенный вызов, который делает revert, возвращается к своему вызывающему как обычный **проваленный вызов**, несущий свою причину, — а не убивающий всю цепочку взрыв. Собственные изменения состояния вызываемого откатываются, управление возвращается вызывающему, и тот решает: пробросить ошибку выше или обработать её. Это в точности тот механизм, что стоит за обработкой ошибок, которую вы пишете каждый день.

<Scene name="rev-catch" lang="ru" caption="Когда вызываемый делает REVERT, провал возвращается к вызывающему как перехватываемый результат с прикреплённой причиной — состояние вызываемого откатывается, но вызывающий продолжает работу и сам решает, что делать. Именно на этом построены require(reason), кастомные ошибки и try/catch в Solidity." />

<Callout type="insight" title="Вот что такое REVERT">
Раньше прервать вызов означало **throw**: он откатывал состояние, но сжигал весь оставшийся газ и не возвращал ничего → **REVERT** сохраняет атомарный откат, но **возвращает** неиспользованный газ и **отдаёт** блок данных с ошибкой → а поскольку он возвращает данные как обычный проваленный вызов, revert вложенного вызова **перехватываем**, причём с сохранённой причиной. На этом единственном опкоде держится вся современная модель обработки ошибок: `require(cond, "reason")`, `revert CustomError(...)` (дешевле строк) и `try/catch` в Solidity. Провал превратился из тихого, опустошающего кошелёк тупика в дешёвый, точный, составимый сигнал. В тот же день Byzantium принёс ещё два знаковых изменения — **STATICCALL** (вызов, гарантированно не меняющий состояние, для безопасного чтения) и прекомпайлы спаривания **alt_bn128** ([EIP-196](https://eips.ethereum.org/EIPS/eip-196)/[197](https://eips.ethereum.org/EIPS/eip-197)), которые впервые сделали практичной верификацию **доказательств с нулевым разглашением** on-chain, положив начало всему — от приватных платежей до сегодняшних zk-роллапов, — но именно REVERT вы трогаете каждый раз, когда пишете проверку.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-140: REVERT instruction", note: "Спецификация — 0xfd, возврат газа, return-данные", href: "https://eips.ethereum.org/EIPS/eip-140" },
    { kind: "post", label: "Глава 2 — EVM, газ и вызовы", note: "Фреймы, газ и модель вызовов, на которых это строится", href: "/the-evm" },
    { kind: "eip", label: "EIP-211: RETURNDATASIZE / RETURNDATACOPY", note: "Как вызывающий на самом деле читает возвращённую причину", href: "https://eips.ethereum.org/EIPS/eip-211" },
    { kind: "eip", label: "EIP-197: alt_bn128 pairing precompile", note: "Другое наследие Byzantium — верификация zk on-chain", href: "https://eips.ethereum.org/EIPS/eip-197" },
    { kind: "tool", label: "evm.codes — REVERT (0xfd)", note: "Опкод в интерактивном справочнике", href: "https://www.evm.codes/#fd" },
  ]}
/>

</DepthLayer>
