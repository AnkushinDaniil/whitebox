---
title: "EIP-1153: черновик, который живёт ровно одну транзакцию"
dek: "Контрактам часто нужно передавать данные между вызовами внутри одной транзакции — блокировки от реентрантности, временные апрувы, флэш-бухгалтерия. Единственным общим изменяемым хранилищем было постоянное storage: ≈20 000 газа за слот, записанное на диск навсегда, хотя значение выбрасывается по окончании транзакции. EIP-1153 добавляет временное хранилище (transient storage) — та же идея, но исчезает в конце транзакции."
summary: "Пошаговая деривация EIP-1153 (transient storage) — почему передача изменяемых данных между вызовами внутри одной транзакции вынуждала контракты злоупотреблять постоянными SSTORE/SLOAD (≈20 000 газа на установку слота, запись в state trie навсегда, затем сброс в ноль с опорой на урезанные возвраты газа), и как transient storage исправляет это двумя опкодами, TSTORE и TLOAD: хранилище slot→word на контракт, дешёвое и с фиксированным газом, никогда не записываемое на диск, общее для вызовов и подвызовов внутри транзакции, откатываемое при реверте фрейма и автоматически отбрасываемое (обнуляемое) по окончании транзакции — что делает защиту от реентрантности, временные апрувы и флэш-бухгалтерию дешёвыми и чистыми, без раздувания состояния."
topic: EIPs
order: 12
spine: true
prerequisites: ["storage против memory", "модель вызовов EVM"]
updated: 2026-06-23
readingMinutes: 10
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

EVM даёт контракту два места для хранения данных, и ни одно не подходит для распространённой нужды. **Memory (память)** дешёвая, но стирается в конце каждого *вызова* — она не может пронести что-либо через подвызов. **Storage (хранилище)** переживает вызовы (и остаётся навсегда после), но это самый дорогой ресурс в EVM, потому что записывается на диск. При этом контрактам постоянно нужна *третья* вещь: место, куда можно спрятать данные, которые переживают вызовы, **но только в рамках текущей транзакции**, — блокировка от реентрантности, временный апрув, флэш-бухгалтерия в AMM. Имея под рукой только storage, они платят цену диска за стикер-напоминание. **EIP-1153** добавляет ровно недостающий инструмент. Давайте его выведем.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Платим цену диска за записку" },
  { id: "step-2", label: "Черновик на одну транзакцию" },
  { id: "step-3", label: "TSTORE и TLOAD" },
  { id: "step-4", label: "Revert и автоочистка" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Платим цену диска за записку">

## Проблема: storage — единственное общее изменяемое хранилище

Возьмите классическую **защиту от реентрантности (reentrancy guard)**: установите флаг `locked` перед внешним вызовом, снимите его после и отказывайте в повторном входе, пока он установлен. Флаг должен быть виден *реентрантному* вызову, поэтому он не может жить в memory — он обязан лежать в <KeyTerm term="постоянное хранилище (persistent storage)" def="Постоянное хранилище контракта вида ключ→значение, читаемое/записываемое через SLOAD/SSTORE. Оно фиксируется в state trie и живёт на диске каждого узла навсегда, поэтому запись в него — самая дорогая распространённая операция EVM." />. Поэтому вы делаете `SSTORE` флага (≈20 000 газа, чтобы установить из нуля, ≈5 000 — чтобы обновить), выполняете работу, затем `SSTORE` обратно в ноль — при каждом отдельном вызове. Вы пишете на диск каждого узла, *навсегда*, чтобы удержать значение, чей весь полезный срок жизни — несколько микросекунд внутри одной транзакции.

<Scene name="ts-persist" lang="ru" caption="Защита от реентрантности устанавливает слот storage в 1, выполняет защищённое тело, затем сбрасывает его в 0 — платя 20k/5k газа SSTORE и записывая state trie на диск ради флага, который теряет смысл в момент окончания транзакции." />

Хуже того: половина с «сбросом в ноль» опирается на **возвраты газа (gas refunds)**, чтобы отыграть часть стоимости, но [EIP-3529](https://eips.ethereum.org/EIPS/eip-3529) резко урезал и ограничил эти возвраты (ими злоупотребляли как газ-токенами). Так что паттерн дорогой, косвенный и навсегда раздувает состояние значениями, которые больше никому никогда не понадобятся.

<Callout type="warning" title="Что ломается">
Мы используем постоянное, опирающееся на диск хранилище для данных, которые должны испаряться по окончании транзакции. На самом деле нам нужно хранилище с *разделяемостью* storage, но с *временем жизни* одной транзакции.
</Callout>

**→ Шаг 2: дать контрактам черновик.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Черновик на одну транзакцию">

## Добавляем второе хранилище, которое живёт одну транзакцию

Исправление — перестать перегружать storage и добавить параллельное хранилище с другим временем жизни. <KeyTerm term="временное хранилище (transient storage)" def="Второе хранилище на контракт вида ключ→слово, введённое EIP-1153. Оно ведёт себя как storage внутри транзакции, но отбрасывается (сбрасывается в ноль) по окончании транзакции и никогда не записывается в state trie или на диск." /> — это привязанная к контракту карта `slot → 32-байтовое слово`, точно как storage, — за исключением того, что она **никогда не записывается в state trie или на диск**, и она **обнуляется целиком по окончании транзакции**. Поскольку ничего не сохраняется постоянно, не нужно платить за запись на диск, и нет постоянного следа: это черновик, который протокол выдаёт каждому контракту на время одной транзакции, а затем уничтожает.

<Scene name="ts-scratch" lang="ru" caption="Постоянное storage (SSTORE/SLOAD) фиксируется на диске и хранится вечно; временное хранилище (TSTORE/TLOAD) живёт только внутри транзакции и отбрасывается по её окончании. Та же модель слотов на контракт, совершенно другое время жизни." />

Именно это единственное изменение — *не сохранять постоянно* — делает его дешёвым. Стоимость `SSTORE` определяется в первую очередь диском и вызванным им ростом состояния; уберите их — и операция становится плоской записью по цене «тёплого» доступа.

<Callout type="warning" title="Что ломается">
Нам нужно реально предоставить это контрактам: как они его читают и пишут, сколько это стоит и — что критично — может ли подвызов увидеть то, что записал внешний вызов?
</Callout>

**→ Шаг 3: два опкода, общих для всего стека вызовов.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="TSTORE и TLOAD">

## Два опкода — и они общие для всех вызовов

Transient storage получает два новых опкода, зеркальных паре storage: <KeyTerm term="TSTORE / TLOAD" def="TSTORE(key, value) записывает слово во временное хранилище; TLOAD(key) читает его. Оба имеют фиксированную стоимость (100 газа, как «тёплый» доступ к storage) без механизма возвратов и оперируют собственным временным пространством имён вызывающего контракта." /> — `TSTORE(key, value)` и `TLOAD(key)`. Каждый стоит плоские **100 газа** (цена «тёплого» доступа), без учёта возвратов, о котором нужно думать. И, как и постоянное storage, временное хранилище **сохраняется через фреймы вызовов транзакции**: если вызывающий делает `TSTORE(k, v)`, а затем совершает подвызов, `TLOAD(k)` у вызываемого прочитает обратно `v`. Эта видимость между вызовами — вся суть: именно она позволяет реентрантному внутреннему вызову увидеть блокировку, установленную внешним фреймом, или роутеру спрятать контекст, который прочитает колбэк.

<Scene name="ts-opcodes" lang="ru" caption="Вызывающий пишет через TSTORE(k, v); подвызов читает то же значение через TLOAD(k). Каждая операция стоит плоские ~100 газа в собственном пространстве имён контракта, без возвратов — и значение автоматически очищается по окончании транзакции." />

<SpecExcerpt lang="ru"
  source="EIP-1153: Transient storage opcodes"
  href="https://eips.ethereum.org/EIPS/eip-1153"
  ref="семантика и газ"
>
`TLOAD` (0x5C) и `TSTORE` (0x5D) действуют на временное хранилище, адресуемое, как storage, парой
`(address, key)`. Значения отбрасываются по окончании транзакции, ведя себя так, будто
в её начале они равны нулю. Газ — `100` за каждую операцию, без возвратов и без учёта чистого газа.
Временное хранилище независимо от постоянного storage.
</SpecExcerpt>

<Formula
  lang="ru"
  expr="gas(TSTORE) = gas(TLOAD) = 100"
  terms={[
    { t: "TSTORE", note: "записать слово во временное хранилище" },
    { t: "TLOAD", note: "прочитать его обратно — между вызовами, в рамках одной транзакции" },
    { t: "100", note: "плоская «тёплая» цена; без возвратов, и диск никогда не затрагивается" },
  ]}
  caption="Дёшево, потому что ничего не сохраняется постоянно — каждый временный слот стирается по окончании транзакции."
/>

<Callout type="warning" title="Что ломается">
Если подвызов пишет во временное хранилище, а затем *реверится*, останется ли эта запись, чтобы запутать остаток транзакции? Защита от реентрантности была бы небезопасна, если бы не разворачивалась корректно.
</Callout>

**→ Шаг 4: заставляем его следовать правилам revert и конца транзакции.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Revert и автоочистка">

## Он откатывается при revert и сам очищается по окончании транзакции

Два правила очистки делают это безопасным. Во-первых, временное хранилище подчиняется **revert** точно так же, как постоянное storage: если фрейм вызова реверится, каждый сделанный им `TSTORE` откатывается, как будто его никогда не было. (Это правило добавили в ходе стандартизации именно для того, чтобы защиты от реентрантности оставались корректными — блокировка, установленная во фрейме, который затем реверится, не должна оставаться застрявшей включённой.) Во-вторых, когда **транзакция заканчивается**, всё временное хранилище автоматически сбрасывается в ноль. Так что защите от реентрантности больше вообще не нужен завершающий сброс `SSTORE(slot, 0)` — очистка бесплатна и автоматична.

<Scene name="ts-revert" lang="ru" caption="TSTORE во фрейме, который реверится, отменяется — точно как storage; а по окончании транзакции каждый временный слот сам сбрасывается в ноль — так что защите не нужен явный сброс, и ничего не просачивается в следующую транзакцию." />

<Callout type="insight" title="И это EIP-1153">
Контрактам нужны были данные, общие для вызовов, но только в рамках одной транзакции → единственным общим изменяемым хранилищем было постоянное **storage**, так что они платили ≈20k/5k газа и постоянные записи на диск за одноразовый флаг, опираясь на урезанные возвраты → добавляем параллельное **временное хранилище**: та же модель слотов на контракт, но никогда не сохраняется постоянно и обнуляется по окончании транзакции → предоставляем через **TSTORE/TLOAD** по плоским 100 газа, общее для всего стека вызовов, так что вызываемый видит записи вызывающего → и делаем безопасным через **откат при revert** и **автоочистку по окончании транзакции**. Результат: защиты от реентрантности, временные апрувы ERC-20 и флэш-бухгалтерия AMM (Uniswap v4) становятся дешёвыми и чистыми, без раздувания состояния и игр с возвратами. Урок в том, что *время жизни* — это ось дизайна: memory живёт вызов, storage — вечно, а временное хранилище заполняет очевидно недостающую середину — «живёт транзакцию».
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-1153: Transient storage opcodes", note: "Спецификация — семантика и газ TSTORE/TLOAD", href: "https://eips.ethereum.org/EIPS/eip-1153" },
    { kind: "eip", label: "EIP-3529: Reduction in refunds", note: "Почему трюк со сбросом SSTORE перестал окупаться", href: "https://eips.ethereum.org/EIPS/eip-3529" },
    { kind: "post", label: "EIP-2929/2930, derived", note: "«Тёплый» и «холодный» доступ — газовая модель, которую заимствует TSTORE", href: "/eip-2929-2930-access-lists" },
    { kind: "code", label: "Uniswap v4 — flash accounting", note: "Флагманский пример использования transient storage", href: "https://github.com/Uniswap/v4-core" },
    { kind: "tool", label: "evm.codes", note: "TSTORE (0x5D) / TLOAD (0x5C) в справочнике опкодов", href: "https://www.evm.codes/" },
  ]}
/>

</DepthLayer>
