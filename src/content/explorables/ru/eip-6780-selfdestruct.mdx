---
title: "EIP-6780: обезвреживаем SELFDESTRUCT"
dek: "SELFDESTRUCT мог стереть код контракта и освободить его адрес — что позволяло переразвернуть другой контракт по тому же адресу («метаморфный» контракт) и превращало «контракт может просто исчезнуть» в головную боль для всего дизайна состояния Ethereum. Полное удаление опкода сломало бы существующее, поэтому EIP-6780 его ограничивает: он по-настоящему удаляет только если контракт был создан в той же транзакции."
summary: "Пошаговая деривация EIP-6780 (ограничение SELFDESTRUCT), выпущенного в Cancun (март 2024). Почему исходный SELFDESTRUCT — удалявший код контракта, очищавший его storage и освобождавший его адрес — был опасен: в сочетании с CREATE2 он позволял метаморфные контракты (переразвернуть другой код по тому же адресу, так что аудированный адрес мог незаметно измениться), а способность удалить произвольное состояние одной инструкцией сильно осложняла дорожную карту к бессостоятельности и деревьям Verkle; почему его нельзя было просто убрать (некоторые контракты от него зависят); и как EIP-6780 сохраняет полное удаление только когда контракт делает selfdestruct в той же транзакции, в которой был создан, а иначе лишь переводит свой баланс, оставляя код и storage нетронутыми — убивая метаморфные контракты, сохраняя легитимные помощники в рамках одной транзакции и упрощая семантику состояния."
topic: EIPs
order: 24
spine: true
prerequisites: ["контракты, код и storage", "CREATE2"]
updated: 2026-06-24
readingMinutes: 9
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**Cancun, март 2024.** Две более ранние главы оставили незавязанную нить. Глава про [CREATE2](/eip-1014-create2) предупреждала о *метаморфных* контрактах — размещении разного кода по одному адресу, — а [сокращение возвратов](/eip-3529-refund-reduction) уже лишило `SELFDESTRUCT` возврата газа. Эта глава завязывает нить. `SELFDESTRUCT` был одной из самых опасных инструкций EVM: единственный опкод, способный **полностью стереть контракт** — код исчезает, storage очищается, адрес освобождается. Эта сила открывала настоящую поверхность атаки и тихо блокировала долгосрочные планы Ethereum. **EIP-6780** обезвреживает её, не ломая контракты, которые используют её легитимно. Давайте выведем это.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Стереть и переиспользовать" },
  { id: "step-2", label: "Ограничить, а не убрать" },
  { id: "step-3", label: "Метаморфность мертва" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Стереть и переиспользовать">

## Проблема: контракт мог по-настоящему исчезнуть

Исходный `SELFDESTRUCT` делал три радикальные вещи одновременно: удалял **код** контракта, очищал его **storage** и освобождал **адрес** — отправляя оставшийся баланс получателю. Освобождение адреса — опасная часть, потому что [CREATE2](/eip-1014-create2) позволяет развернуться по *выбранному* адресу. Свяжите их в цепочку — и получите <KeyTerm term="метаморфный контракт (metamorphic contract)" def="Контракт, меняющий свой код по фиксированному адресу: разверните код через CREATE2, сделайте SELFDESTRUCT, чтобы освободить адрес, затем снова CREATE2 по тому же адресу с другим кодом. Ломает предположение о том, что код по адресу неизменен." />: развернуть, самоуничтожиться, переразвернуть *другой* код по *тому же* адресу. Адрес, который кто-то аудировал или которому доверял, мог незаметно стать чем-то совершенно иным.

<Scene name="sd-danger" lang="ru" caption="SELFDESTRUCT стирал код и storage контракта и освобождал его адрес; CREATE2 затем мог переразвернуть другой код по тому же адресу — метаморфный контракт. Адрес, который вы аудировали, мог незаметно изменить то, что он делает." />

Есть вторая, более тихая цена. Опкод, способный удалить произвольное состояние одним выстрелом, — кошмар для дорожной карты к <KeyTerm term="бессостоятельность (statelessness)" def="Направление для Ethereum, где клиенты верифицируют блоки, используя компактные доказательства (свидетельства), затрагивающие только то состояние, которого касается блок, вместо хранения всего состояния. Деревья Verkle — структура, делающая это возможным. Опкод, способный удалить целые аккаунты, сильно усложняет генерацию таких доказательств и рассуждения о них." /> и деревьев Verkle: если любой аккаунт может подмигнуть и исчезнуть из существования, генерация свидетельств состояния и рассуждения о них сильно усложняются. «Контракт может исчезнуть» осложняло всё, что строилось на этом дальше.

<Callout type="warning" title="Что ломается">
Чистое исправление — убрать `SELFDESTRUCT` полностью, — но некоторые развёрнутые контракты по-настоящему на него полагаются, а его выдёргивание могло бы заблокировать средства или сломать живые системы. Нам нужно нейтрализовать *опасное* поведение (удаление кода, освобождение адреса), сохранив работающими *безобидные* случаи использования.
</Callout>

**→ Шаг 2: сохраняем полную силу только там, где безопасно.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Ограничить, а не убрать">

## EIP-6780: полное удаление только в создающей транзакции

Инсайт в том, что *единственный* по-настоящему безопасный момент, чтобы полностью удалить контракт, — это когда он был **только что создан и никогда не покидал транзакцию**, — распространённый паттерн для одноразового помощника, развёрнутого и снесённого в рамках одного вызова. Поэтому EIP-6780 сохраняет полное поведение «удалить код + очистить storage + освободить адрес» **только если контракт был создан в той же транзакции**, что и `SELFDESTRUCT`. Во всех остальных случаях — обычном, когда долгоживущий контракт делает selfdestruct позже, — `SELFDESTRUCT` теперь лишь **переводит баланс** получателю и оставляет **код и storage на месте**. Контракт продолжает существовать.

<Scene name="sd-restrict" lang="ru" caption="После EIP-6780 SELFDESTRUCT полностью удаляет только если контракт был создан в той же транзакции (безопасные одноразовые помощники по-прежнему работают). Уничтоженный в любой более поздней транзакции — обычный случай — он просто отправляет баланс прочь и оставляет код и storage нетронутыми." />

<SpecExcerpt lang="ru"
  source="EIP-6780: SELFDESTRUCT only in same transaction"
  href="https://eips.ethereum.org/EIPS/eip-6780"
  ref="новое поведение"
>
`SELFDESTRUCT` удаляет аккаунт (код и storage) и переводит баланс только если он
вызван в той же транзакции, в которой контракт был создан. В противном случае он
переводит баланс получателю, но **не** удаляет аккаунт и не очищает его
storage — контракт продолжает существовать.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Контракт, делающий selfdestruct после своей создающей транзакции, больше не исчезает — он просто теряет свой баланс. Достаточно ли этого, чтобы реально закрыть метаморфную дыру?
</Callout>

**→ Шаг 3: да — и дорожной карте становится легче дышать.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Метаморфность мертва">

## Выигрыш: больше никаких меняющих форму адресов

Метаморфному трюку требовалось, чтобы `SELFDESTRUCT` *освобождал адрес между транзакциями*, чтобы более поздний `CREATE2` мог его занять. Теперь, когда развёрнутый контракт делает selfdestruct в более поздней транзакции, его **код остаётся**, а адрес остаётся занятым — так что `CREATE2` не может разместить там новый код. Метаморфные контракты фактически мертвы: код по аудированному адресу больше нельзя подменить у вас из-под ног. А поскольку контракты больше не исчезают, семантика состояния становится драматически проще, устраняя препятствие на пути к бессостоятельности. Тем временем легитимный паттерн «создать и уничтожить в одной транзакции» не тронут, так что ничего реального не ломается.

<Scene name="sd-result" lang="ru" caption="Контракт, делающий selfdestruct в более поздней транзакции, сохраняет свой код, так что его адрес остаётся занятым, и CREATE2 не может переразвернуться поверх него — метаморфные контракты мертвы. То, что контракты никогда по-настоящему не исчезают, также упрощает работу с состоянием для бессостоятельности, а помощники в рамках одной транзакции по-прежнему работают." />

<Callout type="insight" title="И это EIP-6780">
`SELFDESTRUCT` удалял код, очищал storage и освобождал адрес → освобождение адреса позволяло **CREATE2** переразвернуть там *другой* код (метаморфные контракты), а «контракт может исчезнуть» блокировало дорожную карту к бессостоятельности → но убрать его совсем нельзя (некоторые контракты от него зависят) → поэтому **ограничиваем** его: полное удаление только в транзакции, где контракт был создан; в остальных случаях он просто отправляет свой баланс и оставляет код на месте. Метаморфные контракты умирают, одноразовые помощники в рамках одной транзакции продолжают работать, а о состоянии становится проще рассуждать. Это финальная точка многофоркового сворачивания — [EIP-3529](/eip-3529-refund-reduction) забрал его возврат газа, затем Cancun забрал его зубы, — опкод обезврежен, а не удалён, так что экосистема смогла двигаться дальше без резкого разрыва.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-6780: SELFDESTRUCT only same-tx", note: "Спецификация — ограниченное поведение", href: "https://eips.ethereum.org/EIPS/eip-6780" },
    { kind: "post", label: "EIP-1014 — CREATE2", note: "Вторая половина метаморфного трюка", href: "/eip-1014-create2" },
    { kind: "post", label: "EIP-3529, derived", note: "Где SELFDESTRUCT потерял свой возврат газа", href: "/eip-3529-refund-reduction" },
    { kind: "tool", label: "Деревья Verkle и бессостоятельность", note: "Почему «контракты никогда не исчезают» важно для будущего", href: "https://ethereum.org/en/roadmap/verkle-trees/" },
    { kind: "tool", label: "evm.codes — SELFDESTRUCT (0xFF)", note: "Опкод и его семантика после 6780", href: "https://www.evm.codes/#ff" },
  ]}
/>

</DepthLayer>
