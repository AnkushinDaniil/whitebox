---
title: "Состояние и trie: один хеш для всего мира"
dek: "EVM вычисляет над хранилищем — но где это хранилище на самом деле живёт, и как сеть незнакомцев соглашается, что оно побайтово идентично, не пересылая друг другу гигабайты? Эта глава выводит дерево Меркла-Патриции: структуру, которая сжимает всё мировое состояние в один 32-байтовый корень, который можно дёшево обновлять и против которого можно доказывать."
summary: "Деривация дерева состояния Ethereum с нуля. Начиная с необходимости для каждого узла соглашаться на идентичном мировом состоянии, мы изобретаем дерево Меркла-Патриции, исправляя один недостаток за раз: у простой карты нет отпечатка; один хеш фиксирует, но вынуждает полное перехеширование и ничего не доказывает по отдельности; дерево Меркла даёт дешёвые обновления и доказательства включения; превращение самого ключа в путь даёт канонический trie; а схлопывание неветвящихся участков даёт дерево Меркла-Патриции. Разбираются типы узлов leaf/extension/branch, почему ключи хешируются в ниббл, как изменение одного баланса перехеширует только его путь до нового корня состояния, и как этот корень оказывается в заголовке блока, так что согласие по заголовку означает согласие по всему состоянию."
topic: "Start here"
order: 1
spine: true
prerequisites: ["EVM и хранилище", "хеширование (keccak256)"]
updated: 2026-09-13
readingMinutes: 15
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import WidgetFrame from "@/components/explorable/WidgetFrame.astro";
import Formula from "@/components/explorable/Formula.astro";
import Bleed from "@/components/explorable/Bleed.astro";
import MptDerivation from "@/components/trie/MptDerivation.tsx";
import ScrollyTrie from "@/components/trie/ScrollyTrie.tsx";

[Глава 2](/the-evm) оставила нить незавершённой. `storage` в EVM — единственное место, где переживают данные контракта — было, как мы сказали, *«зафиксировано в дереве состояния и хранится на диске каждого узла»*. Но что такое это trie и почему именно такая форма? Настоящая проблема под этим — суровая: сотни тысяч машин, которыми управляют незнакомцы, каждая хранит **всё** состояние Ethereum — каждый баланс аккаунта, nonce, контракт и слот хранилища — и они должны соглашаться, что оно *в точности* одинаково, блок за блоком. Этого нельзя добиться, пересылая друг другу гигабайты по почте и сравнивая их. Эта глава выводит структуру, которая делает это возможным — так же, как мы выводим всё остальное: начинаем с самой тупой вещи, которая могла бы сработать, и чиним то, что ломается.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Один хеш на всё" },
  { id: "step-2", label: "Выводим структуру" },
  { id: "step-3", label: "Три типа узлов" },
  { id: "step-4", label: "Обновление, от начала до конца" },
  { id: "step-5", label: "Он попадает в заголовок" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Один хеш на всё">

## Проблема: дёшево доказать, что два мира идентичны

<KeyTerm term="мировое состояние" def="Полное отображение от каждого адреса аккаунта к его состоянию — баланс, nonce, а для контрактов ещё хеш кода и хранилище. Именно это преобразует исполнение блока; каждый полный узел хранит всё это." /> огромно и постоянно меняется. Двум узлам нужен способ убедиться, что они хранят идентичное состояние, не пересылая его целиком — и, отдельно, способ доказать *один* факт (*«аккаунт 0xa7… держит 12 ETH»*) тому, у кого состояния нет вовсе. Обе потребности указывают на один и тот же инструмент: **криптографический отпечаток**. Хешируем всё состояние в одно 32-байтовое число, и если числа двух узлов совпадают, совпадают и их состояния; измените хоть один вей где угодно — и число перевернётся.

<Scene name="st-fingerprint" lang="ru" caption="Хешируем всё мировое состояние в единый 32-байтовый корень. Два узла с одним и тем же корнем хранят одно и то же состояние; переверните один вей в любом аккаунте — и корень изменится, так что несогласие видно мгновенно. Именно этот единственный отпечаток — то, о чём договаривается вся сеть." />

<Callout type="warning" title="Что ломается">
Хеширование состояния как одного гигантского блоба даёт нам отпечаток — и ничего больше. Измените один баланс — и придётся перехешировать *всё*. А чтобы доказать один аккаунт лёгкому клиенту, вам пришлось бы передать всё состояние целиком, чтобы он мог воспроизвести хеш. Оба варианта безнадёжны при масштабе Ethereum. Нам нужно, чтобы отпечаток был *структурированным*.
</Callout>

**→ Шаг 2: заработаем структуру, исправляя по одному недостатку за раз.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Выводим структуру">

## От простой карты к дереву Меркла-Патриции

Смотрите, как структура строит сама себя. Каждый этап ниже чинит недостаток предыдущего — у простой карты нет вообще никакого обязательства; хеширование её в одно число фиксирует, но не позволяет ни доказывать, ни дёшево обновлять; хеширование *в дереве* означает, что изменение перехеширует лишь один путь *и* любой лист можно доказать его веткой; превращение **самого ключа** в маршрут вниз по дереву делает форму канонической (все строят идентичное дерево); а схлопывание длинных неветвящихся участков убирает лишние узлы. В итоге получается <KeyTerm term="дерево Меркла-Патриции" def="Структура состояния Ethereum: радиксное дерево, ключом которого служит путь через данные, где каждый узел также фиксирует хеш своих потомков (Меркл), а неветвящиеся пути сжаты (Патриция). Даёт единый корневой хеш, обновления за O(log n) и компактные доказательства включения." /> (Merkle-Patricia trie).

<WidgetFrame
  lang="ru"
  title="Выводим дерево Меркла-Патриции"
  caption="Пройдите пять этапов: простая карта → один хеш → дерево Меркла → trie с ключом-как-путём → MPT со сжатием по Патриции. Каждый этап чинит ровно один недостаток предыдущего."
  source="Настоящее построение trie — та же структура, что строят клиенты Ethereum, показанная поэтапно."
>
  <MptDerivation client:visible />
</WidgetFrame>

Стоит назвать два свойства, потому что на них опирается всё дальнейшее. <KeyTerm term="корень Меркла" def="Хеш на вершине дерева хешей, где каждый узел хеширует своих потомков. Поскольку хеш распространяется вверх, единственный корень фиксирует каждый лист под собой — а короткий список хешей-соседей (ветка) доказывает любой один лист." /> фиксирует каждый лист через цепочку хешей потомков, так что один корень закрепляет весь набор данных. И поскольку *ключ* определяет *путь*, дерево **каноническое**: имея одни и те же аккаунты, каждый клиент независимо строит побайтово одинаковый trie и, значит, одинаковый корень. Никакого выбора порядка, никакой неоднозначности.

<Callout type="warning" title="Что ломается">
«Сделать ключ путём» — за этой фразой скрывается очень многое. Как на самом деле выглядит узел этого дерева — и как сохранить его компактным, если большинство путей через него вообще не ветвятся?
</Callout>

**→ Шаг 3: три типа узлов.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Три типа узлов">

## Leaf, extension, branch — и почему их ровно три

Ключ сначала хешируется, а затем читается по 4 бита за раз. Каждый 4-битный кусок — это <KeyTerm term="ниббл" def="Половина байта — одна шестнадцатеричная цифра, 0–f. Ключи trie обходятся по одному ниббл за раз, так что в каждой точке ветвления есть шестнадцать возможных направлений — по одному на значение ниббла." /> (nibble), так что в любой точке ветвления есть шестнадцать возможных направлений. Это и задаёт нужные типы узлов: <KeyTerm term="ветвящийся узел" def="Узел с 17 слотами — по одному указателю на потомка на каждый ниббл (0–f) плюс слот значения. Используется там, где пути действительно расходятся." /> (branch node) для мест, где пути реально расходятся, <KeyTerm term="узел-расширение" def="Узел, хранящий общую последовательность ниббл и один указатель на потомка — сжатие по Патриции, схлопывающее длинный неветвящийся участок в один узел." /> (extension node), чтобы схлопнуть общий участок ниббл в один узел, и <KeyTerm term="листовой узел" def="Узел, хранящий оставшиеся ниббл ключа и его значение — конец пути." /> (leaf node), чтобы хранить хвост ключа и его значение.

<Scene name="st-nodes" lang="ru" caption="Проход по ключу a7c1: узел-расширение поглощает общий префикс «a7», 16-сторонний ветвящийся узел разделяется на следующем ниббле «c», а листовой узел хранит финальный «c1» и значение. Каждый узел также хранит хеши своих потомков — этот слой Меркла и делает так, что корень зависит от каждого значения под собой." />

Тот факт, что «каждый узел также хранит хеши своих потомков» — это тихая, но несущая деталь: именно она сплавляет обычное маршрутизирующее дерево (Патриция) с деревом хешей (Меркл). Маршрут делает поиск детерминированным; хеши делают всю конструкцию заметно защищённой от подделки вплоть до корня.

<Formula
  lang="ru"
  expr="path = nibbles( keccak256(address) )"
  terms={[
    { t: "keccak256(address)", note: "сначала хешируем ключ — это равномерно распределяет ключи, так что trie остаётся неглубоким и сбалансированным" },
    { t: "nibbles", note: "делим на 4-битные куски; каждый ниббл выбирает одно из 16 направлений ветвления" },
  ]}
  caption="Ключи хешируются, а затем обходятся по одному ниббл за раз — так что путь вниз по trie определяется самим ключом."
/>

<SpecExcerpt
  lang="ru"
  source="Ethereum Yellow Paper, §4.1 & Appendix D — World State & the Trie"
  href="https://ethereum.github.io/yellowpaper/paper.pdf"
  ref="состояние как trie"
>
Мировое состояние — это отображение между адресами и состояниями аккаунтов,
поддерживаемое как модифицированное дерево Меркла-Патриции (`trie`). Trie ключуется
по `keccak256(address)`, а его значения — это RLP-закодированные состояния аккаунтов
`(nonce, balance, storageRoot, codeHash)`. Хеш корневого узла — корень состояния —
является надёжным обязательством по всему состоянию и записывается в заголовок блока.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
У нас есть структура с одним корнем. Теперь настоящее испытание: транзакция меняет один баланс. Приходится ли перестраивать всё дерево целиком — или именно здесь форма дерева наконец окупается?
</Callout>

**→ Шаг 4: меняем один баланс и смотрим, как это распространяется.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Обновление, от начала до конца">

## Один баланс меняется; перехешируется только его путь

Вот и вознаграждение за всю эту структуру. Измените баланс одного аккаунта — и вы *не* трогаете остальную часть trie. Вы переписываете лист, а затем идёте **вверх** по его пути, перехешируя каждого родителя — потому что хеш узла зависит от его потомков, изменившийся потомок вынуждает новый хеш вплоть до самого корня. Каждый узел, *не* лежащий на этом пути, сохраняет свой старый хеш нетронутым и просто переиспользуется. Пролистайте это:

<Bleed>
  <ScrollyTrie client:visible />
</Bleed>

Так что обновление имеет сложность **O(глубины)**, а не O(размера состояния): горстка хешей, а не миллионы. Тот же самый путь — лист плюс хеши-соседи по дороге — это в точности <KeyTerm term="доказательство Меркла" def="Лист плюс хеши-соседи на пути к корню. Любой, у кого есть только корень, может пересчитать всё вверх и подтвердить, что лист включён — доказывая один аккаунт без остального состояния." /> (Merkle proof): передайте его тому, у кого есть только корень, и он сможет проверить этот один аккаунт, без остального состояния. Дешёвые обновления и компактные доказательства — это *одно и то же* свойство, прочитанное в двух направлениях.

<Callout type="insight" title="Почему именно такая форма, одной строкой">
Trie — это ответ сразу на три требования: **одно** число, о котором договариваются (корень), **дешёвые** обновления (перехешируется только путь) и **портативные** доказательства (ветка доказывает один факт относительно корня). Ни одна плоская структура не даёт всех трёх сразу.
</Callout>

**→ Шаг 5: куда на самом деле уходит корень.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-5" num="5" badge="шаг" title="Он попадает в заголовок">

## Корень состояния в заголовке блока

Весь смысл сжатия состояния в один хеш — в том, что теперь с этим можно сделать. <KeyTerm term="корень состояния" def="Хеш корневого узла дерева состояния — единое 32-байтовое обязательство по всему мировому состоянию после исполнения блока. Хранится в заголовке блока рядом с корнями транзакций и квитанций." /> (state root) становится одним полем в **заголовке блока**, рядом с корнем транзакций и корнем квитанций. Исполните блок, вычислите новый корень состояния и вложите его туда. Теперь согласие по заголовку *идентично* согласию по всему мировому состоянию — несколько сотен байт замещают собой память всей цепочки. И поскольку каждый заголовок также называет хеш своего родителя, изменение любого прошлого состояния изменило бы корень того блока, его хеш и каждый хеш после него.

<Scene name="st-header" lang="ru" caption="Корень состояния — один из трёх корней, к которым обязывается заголовок. Вычислите его после исполнения блока, и он резюмирует всё состояние в 32 байтах; сцепление каждого заголовка с хешем родителя означает, что изменение любого прошлого состояния каскадом прошло бы через каждый последующий хеш блока. Согласитесь по последнему заголовку — и вы согласились по всему." />

<Callout type="insight" title="И это и есть дерево состояния">
Каждый узел должен соглашаться на идентичном мировом состоянии → зафиксируйте его в одном криптографическом **корне** → но плоский хеш нельзя дёшево обновить или им что-то доказать, так что стройте его как дерево **Меркла** → сделайте **ключ путём**, чтобы дерево было каноническим → **сожмите** неветвящиеся участки → получаем дерево Меркла-Патриции, с leaf-, extension- и branch-узлами над хешированными ниббл-ключами. Изменение перехеширует только свой путь (и этот путь *и есть* доказательство), а получившийся **корень состояния** уходит в заголовок блока — так что вся, вечно меняющаяся память компьютера-всего-мира сводится к 32 байтам, которые может проверить телефон. Следующие главы строятся прямо на этом: консенсус соглашается о том, какой заголовок (а значит, какой корень) канонический, а клиенты тихо соревнуются в том, как хранить это trie на настоящих дисках.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "post", label: "Глава 2 — The EVM", note: "Откуда взялось хранилище; здесь оно фиксируется", href: "/the-evm" },
    { kind: "spec", label: "Yellow Paper §4.1 & Appendix D", note: "Мировое состояние и дерево Меркла-Патриции, формально", href: "https://ethereum.github.io/yellowpaper/paper.pdf" },
    { kind: "post", label: "Flat DB — reading state in one hop", note: "Как клиенты на самом деле кладут этот trie на диск", href: "/flat-db-nethermind" },
    { kind: "post", label: "EIP-2929/2930 — cold vs warm access", note: "Почему первое обращение к слоту trie стоит дороже", href: "/eip-2929-2930-access-lists" },
    { kind: "tool", label: "Бинарное дерево состояния (EIP-7864)", note: "Актуальное направление к бессостоятельности — дружественное к SNARK бинарное дерево Меркла, заменившее прежний план с Verkle", href: "https://eips.ethereum.org/EIPS/eip-7864" },
  ]}
/>

</DepthLayer>
