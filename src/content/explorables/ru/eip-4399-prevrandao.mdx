---
title: "EIP-4399: PREVRANDAO — откуда берётся случайность в блокчейне"
dek: "EVM детерминирован — у него нет опкода «случайное число», потому что каждый узел обязан вычислить одинаковый результат. Контракты, которым нужна была случайность, злоупотребляли опкодом DIFFICULTY — слабым и подверженным манипуляциям майнеров, — который исчез после The Merge. EIP-4399 переиспользует этот опкод, чтобы раскрыть RANDAO beacon-цепочки, давая контрактам настоящий (хотя и смещаемый) маяк случайности."
summary: "Пошаговая деривация EIP-4399 (PREVRANDAO), выпущенного вместе с The Merge (Paris, сентябрь 2022). Почему у EVM нет опкода для случайности — детерминизм требует, чтобы каждый узел приходил к одинаковому результату, — и почему контракты злоупотребляли опкодом DIFFICULTY (0x44) как слабым, подверженным смещению майнерами источником случайности, который исчез с окончанием proof of work; как EIP-4399 переиспользует тот же опкод, переименованный в PREVRANDAO, чтобы возвращать предыдущее значение RANDAO beacon-цепочки (случайность, подмешиваемую валидаторами в каждом блоке), давая контрактам 32-байтовый маяк случайности; и ключевая оговорка, что это слабая случайность — предлагающий блока знает значение заранее и может пропустить свой слот, чтобы сместить его, — так что она подходит для низкорисковых применений, а высокоценным приложениям стоит использовать схемы commit-reveal или проверяемую функцию случайности."
topic: EIPs
order: 30
spine: true
prerequisites: ["детерминизм EVM", "The Merge и beacon-цепочка"]
updated: 2026-06-24
readingMinutes: 8
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**Paris / The Merge, сентябрь 2022 года.** Вот вопрос, который звучит тривиально, но таким не является: *как смарт-контракт получает случайное число?* На обычном компьютере вы бы вызвали функцию случайности. Но [EVM детерминирован](/the-evm) — каждый узел заново исполняет каждую транзакцию и **обязан** прийти к идентичному результату, иначе узлы разойдутся в состоянии. Настоящий опкод `random()` поэтому невозможен: что бы он ни возвращал, значение должно быть одинаковым на каждой машине, а это противоположность случайности. Контрактам всё равно нужна была случайность, и они хватались за ближайшее, что *выглядело* непредсказуемым, — а The Merge одновременно сломал этот хак и предложил кое-что получше. **EIP-4399** — этот обмен. Давайте его выведем.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Нет случайности в общей машине" },
  { id: "step-2", label: "Раскрываем RANDAO" },
  { id: "step-3", label: "Слабая — обращайтесь осторожно" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Нет случайности в общей машине">

## Проблема: детерминизм запрещает случайность

Поскольку каждый узел обязан вычислять одинаковое состояние, EVM не может иметь настоящий источник случайности — любое произведённое им значение должно быть воспроизводимо для всех. Поэтому опкода для случайности нет, а контракты, которым нужна была непредсказуемость, импровизировали. Излюбленным хаком был опкод `DIFFICULTY` (`0x44`), возвращавший сложность proof of work блока — число, менявшееся от блока к блоку и *казавшееся* непредсказуемым. Но это была **слабая случайность**: майнеры могли её подталкивать, и она не проектировалась как неугадываемая. Хуже того, с The Merge proof of work закончился, и **сложности не стало вовсе** — фундамент хака просто исчез.

<Scene name="pr-det" lang="ru" caption="EVM детерминирован — нет опкода случайности, потому что каждый узел обязан соглашаться. Контракты злоупотребляли опкодом DIFFICULTY как слабой, подверженной смещению майнерами заменой случайности — а конец proof of work на The Merge убрал и это. Но beacon-цепочка уже производит значение случайности: RANDAO." />

<Callout type="warning" title="Что ломается">
Контрактам нужно *какое-то* общее, более-менее непредсказуемое значение, с которым согласен каждый узел. Настоящий опкод случайности невозможен; хак с difficulty исчез. Есть ли уже в протоколе значение, подмешиваемое непредсказуемо и доступное всем?
</Callout>

**→ Шаг 2: да, есть — RANDAO beacon-цепочки.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Раскрываем RANDAO">

## Переиспользуем опкод, чтобы он возвращал RANDAO

Beacon-цепочка уже ведёт общее значение случайности под названием <KeyTerm term="RANDAO" def="Значение случайности, которое beacon-цепочка накапливает, подмешивая вклад от каждого предлагающего блока (раскрытие из его приватной, заранее зафиксированной последовательности). Ни один отдельный валидатор не контролирует результат, и он одинаков для каждого узла." />: в каждом блоке предлагающий раскрывает значение, которое подмешивается (через XOR) в текущий RANDAO, так что ни один валидатор его не контролирует, и каждый узел хранит одно и то же число. EIP-4399 просто **раскрывает его для EVM** — переиспользуя ставший бессмысленным опкод `DIFFICULTY` (`0x44`), переименованный в <KeyTerm term="PREVRANDAO" def="Новое, эпохи The Merge, значение опкода 0x44 (бывшего DIFFICULTY): он возвращает предыдущее значение RANDAO beacon-цепочки — 32-байтовое число, доступное контрактам как маяк случайности." />. Теперь `PREVRANDAO` возвращает RANDAO предыдущего блока — 32-байтовое значение с beacon-цепочки, — так что у контракта есть встроенный маяк случайности, с которым согласен каждый узел, без нового слота опкода или специального механизма.

<Scene name="pr-randao" lang="ru" caption="Beacon-цепочка подмешивает вклад от каждого предлагающего в текущий RANDAO. EIP-4399 переиспользует старый опкод DIFFICULTY (0x44) как PREVRANDAO, возвращающий предыдущий RANDAO, — так что контракт может прочитать 32-байтовое, согласованное сетью значение случайности." />

<SpecExcerpt lang="ru"
  source="EIP-4399: Supplant DIFFICULTY opcode with PREVRANDAO"
  href="https://eips.ethereum.org/EIPS/eip-4399"
  ref="опкод"
>
После The Merge опкод `0x44` (бывший `DIFFICULTY`) возвращает значение `prev_randao` из
beacon-цепочки — накопленную смесь RANDAO предыдущего блока. Он даёт контрактам источник
случайности в цепочке, заменяя ставшее нулевым значение сложности.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
У нас есть общее, нетривиально подмешиваемое значение — лучше, чем хак с difficulty. Но *хороша* ли это случайность? Кто-нибудь может предвидеть её заранее или согнуть в свою пользу?
</Callout>

**→ Шаг 3: знаем в точности, насколько она слаба.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Слабая — обращайтесь осторожно">

## Итог — и острая грань

`PREVRANDAO` — это настоящий, согласованный сетью маяк случайности, и для низкорисковых применений (выбор товара дня, разрешение ничьей, джиттер) он вполне подходит. Но это **не** сильная случайность, и использовать её так, будто это не так, — классическая ошибка. Причина: <KeyTerm term="смещение предлагающим (proposer bias)" def="Предлагающий блока узнаёт значение RANDAO, которое будет применяться к его слоту, на эпоху вперёд, и может решить пропустить (не предлагать) свой слот — теряя вознаграждение, но меняя, какой RANDAO применится. Так у предлагающего есть ограниченное, но реальное влияние на «случайный» результат." /> означает, что сторона, собирающая блок, знает значение заранее и может немного *подшлифовать* его, пропустив слот. Так что для чего-либо ценного — лотереи, крупного случайного распределения — контрактам стоит добавить сверху схему **commit-reveal** или использовать внешнюю **проверяемую функцию случайности** (например, Chainlink VRF). `PREVRANDAO` — это пол, а не потолок.

<Scene name="pr-caveat" lang="ru" caption="PREVRANDAO слаб: предлагающий знает значение заранее и может пропустить слот, чтобы сместить его. Годится, когда небольшое смещение допустимо; для высокоценной случайности добавьте сверху схему commit-reveal или проверяемую функцию случайности (VRF)." />

<Callout type="insight" title="И это и есть PREVRANDAO">
EVM детерминирован, поэтому настоящего опкода случайности нет → контракты злоупотребляли **DIFFICULTY**, который был слаб и исчез с The Merge → beacon-цепочка уже подмешивает общий **RANDAO**, так что переиспользуем мёртвый опкод `0x44` как **PREVRANDAO**, чтобы его раскрыть → давая контрактам согласованный сетью 32-байтовый маяк случайности, с оговоркой, что предлагающий может **сместить его, пропустив слот**, — так что это для низкорисковых применений, а не высокоценных лотерей. Изящная часть — переиспользование опкода, который *уже* был де-факто источником случайности, так что контракты, читавшие `block.difficulty`, продолжали работать со строго лучшим значением под капотом. После The Merge, когда контракт просит «случайность», именно это он и получает — а знание её пределов отличает забавную фичу от эксплойта.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-4399: PREVRANDAO", note: "Спецификация — новое значение 0x44", href: "https://eips.ethereum.org/EIPS/eip-4399" },
    { kind: "post", label: "EIP-3675 — the Merge", note: "Где mixHash стал prevRandao в заголовке", href: "/eip-3675-the-merge" },
    { kind: "post", label: "How consensus runs", note: "RANDAO и случайность валидаторов", href: "/how-consensus-runs" },
    { kind: "post", label: "Chapter 2 — The EVM", note: "Почему детерминизм запрещает опкод случайности", href: "/the-evm" },
    { kind: "tool", label: "Chainlink VRF", note: "Проверяемая функция случайности для высокоценной случайности", href: "https://docs.chain.link/vrf" },
  ]}
/>

</DepthLayer>
