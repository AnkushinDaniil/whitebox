---
title: "EIP-1014: CREATE2 — знать адрес до того, как контракт существует"
dek: "Раньше адрес контракта выводился из nonce развёртывающего аккаунта — счётчика, меняющегося с каждым развёртыванием, так что предсказать, куда попадёт контракт, было невозможно. CREATE2 выводит адрес из развёртывающего, выбранной вами соли и самого кода: без nonce. Теперь можно вычислить адрес, пополнить его и раздать до того, как контракт вообще развёрнут."
summary: "Пошаговая деривация EIP-1014 (CREATE2), выпущенного в форке Constantinople (февраль 2019). Почему исходный опкод CREATE выводит адрес контракта из keccak(sender, nonce) — делая адрес непредсказуемым (nonce тикает с каждым развёртыванием) и непереиспользуемым; как CREATE2 вместо этого вычисляет адрес как keccak256(0xff ‖ sender ‖ salt ‖ keccak256(init_code))[12:], завися только от развёртывающего, выбранной соли и init-кода — без nonce, так что одни и те же входные данные всегда дают один и тот же адрес, и его можно вычислить до развёртывания; и что это открывает: контрфактическое развёртывание (пополнить и использовать адрес до того, как его контракт существует, — стейт-каналы, лениво разворачиваемые кошельки, адреса для абстракции аккаунтов), детерминированные фабрики и ловушку метаморфных контрактов — повторное развёртывание другого кода по тому же адресу."
topic: EIPs
order: 19
spine: true
prerequisites: ["создание контрактов и адреса", "keccak256"]
updated: 2026-06-24
readingMinutes: 10
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

**Constantinople, февраль 2019.** Среди её опкодов был один, читающийся как сноска, — второй способ создать контракт, — но он незаметно открыл контрфактические кошельки, стейт-каналы, детерминированные фабрики и во многом то, как абстракция аккаунтов назначает адреса. Проблема, которую он решает, — загадка курицы и яйца: можно ли узнать адрес контракта до того, как он развёрнут? С исходным CREATE ответ был «нет», и это «нет» блокировало целый класс архитектур. **EIP-1014** — это CREATE2, и он превращает это «нет» в «да». Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Nonce делает адрес непознаваемым" },
  { id: "step-2", label: "Выводим из своих входных данных" },
  { id: "step-3", label: "Используем адрес заранее" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Nonce делает адрес непознаваемым">

## Проблема: адрес CREATE зависит от nonce

Когда контракт (или аккаунт) развёртывает другой с помощью исходного CREATE, адрес нового контракта — это `keccak256(rlp(sender, nonce))[12:]` — хеш того, *кто* его развернул, и его <KeyTerm term="nonce" def="Счётчик для каждого аккаунта: для EOA это число отправленных транзакций; для контракта — число созданных им контрактов. Увеличивается с каждым развёртыванием, так что каждый раз он разный и зависит от порядка." />. Nonce — вот в чём подвох: он растёт с каждым развёртыванием, так что один и тот же развёртывающий, создавая два контракта, получает два разных, зависящих от порядка адреса. Вычислить адрес заранее нельзя, если только вы точно не знаете, сколько контрактов развёртывающий создаст до этого, — а как только контракт занял адрес, его nonce уже ушёл вперёд, так что поместить туда что-то ещё никогда нельзя.

<Scene name="cr-nonce" lang="ru" caption="CREATE выводит адрес из развёртывающего и его nonce, а nonce увеличивается с каждым развёртыванием — так что итоговый адрес постоянно меняется и не может быть известен заранее. Это также значит, что использованный однажды адрес нельзя переиспользовать." />

<Callout type="warning" title="Что ломается">
Многим архитектурам хочется зафиксировать адрес до того, как код заживёт: дать кому-то адрес кошелька, который развернётся только при первом использовании, или адрес канала, который обе стороны пополняют заранее. Nonce делает это невозможным — адрес не определён, пока развёртывание фактически не произойдёт. Нужен адрес, зависящий только от того, что можно зафиксировать заранее.
</Callout>

**→ Шаг 2: заменяем nonce на входные данные, которые вы контролируете.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Выводим из своих входных данных">

## CREATE2: адрес из отправителя, соли и кода

<KeyTerm term="CREATE2" def="Опкод Constantinople (0xf5), который развёртывает контракт по адресу, выведенному из развёртывающего, выбранной вызывающим соли и init-кода — детерминированно, без какой-либо зависимости от nonce." /> полностью отбрасывает nonce и выводит адрес из трёх вещей, которые можно решить заранее: **развёртывающего**, выбранной вами <KeyTerm term="salt" def="32-байтное значение, которое развёртывающий выбирает свободно и подмешивает в вывод адреса CREATE2. Изменение соли даёт другой адрес для того же кода; фиксация соли делает адрес воспроизводимым." /> (соли) и **init-кода** (точного развёртываемого байткода). Поскольку ни одно из них не является движущимся счётчиком, одни и те же входные данные всегда дают один и тот же адрес — и, что важно, вы можете вычислить его, ничего не развёртывая.

<Scene name="cr-create2" lang="ru" caption="CREATE2 хеширует развёртывающего, выбранную соль и init-код — nonce нигде нет. Зафиксируйте эти три параметра, и адрес тоже окажется зафиксирован, так что кто угодно может вычислить, где именно будет жить контракт, ещё до его развёртывания." />

<Formula
  lang="ru"
  expr="address = keccak256( 0xff ‖ sender ‖ salt ‖ keccak256(init_code) )[12:]"
  terms={[
    { t: "0xff", note: "постоянный префикс, не дающий адресам CREATE2 когда-либо столкнуться с адресами CREATE" },
    { t: "sender ‖ salt", note: "развёртывающий и выбранное вами значение — оба известны заранее" },
    { t: "keccak256(init_code)", note: "фиксирует точный байткод; измените код — изменится и адрес" },
  ]}
  caption="Каждый вход — это то, что вы фиксируете заранее, — так что адрес является чистой функцией, которую можно вычислить до развёртывания."
/>

<SpecExcerpt
  lang="ru"
  source="EIP-1014: Skinny CREATE2"
  href="https://eips.ethereum.org/EIPS/eip-1014"
  ref="формула адреса"
>
Добавляет `CREATE2` (`0xf5`). Адрес созданного контракта — это
`keccak256(0xff ++ address ++ salt ++ keccak256(init_code))[12:]`, где `salt` — 32-байтное
значение со стека. В отличие от `CREATE`, адрес не зависит от nonce создателя, поэтому он
детерминирован при известных отправителе, соли и init-коде.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Ничего не ломается — но появляется странная новая сила. Если можно вычислить адрес до того, как по нему появится код, что можно *сделать* с адресом, который пока указывает в никуда?
</Callout>

**→ Шаг 3: используем адрес до того, как контракт стал реальным.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Используем адрес заранее">

## Выигрыш: контрфактическое развёртывание

Вот тот ход, который делает возможным CREATE2. Вы вычисляете адрес, а затем **используете его до развёртывания** — раздаёте, отправляете на него ETH, обращаетесь с ним как с настоящим, — хотя контракта там ещё нет. Это <KeyTerm term="counterfactual deployment" def="Заранее вычислить CREATE2-адрес контракта и взаимодействовать с ним (пополнять, ссылаться на него) до развёртывания — а развернуть только когда понадобится. Существование контракта «контрфактично» до этого момента." /> (контрфактическое развёртывание). Адрес можно пополнять и на него можно ссылаться, пока он пуст; позже тот, кому это нужно, запускает `CREATE2` с той же солью и init-кодом, и контракт материализуется точно по этому адресу — уже с ожидающими его средствами.

<Scene name="cr-counterfactual" lang="ru" caption="Вычислите адрес, пополните и раздайте его, пока он ещё пуст, а затем разверните контракт позже с той же солью и кодом — он появится точно по этому адресу, уже держа ETH, который на него отправили. Развёртывание может даже оплатить первый, кто им воспользуется." />

<Callout type="insight" title="Вот что такое CREATE2">
Адрес CREATE зависел от **nonce** — движущегося счётчика, — так что был непознаваем заранее и непереиспользуем → CREATE2 выводит его из **развёртывающего, соли и init-кода** — всё зафиксировано заранее → так что адрес становится чистой функцией, которую можно вычислить, пополнить и раздать **до того, как контракт существует**. Это единственное изменение лежит в основе **стейт-каналов** (пополнить общий адрес, развернуть при споре), **лениво разворачиваемых кошельков** (ваш смарт-аккаунт сразу получает адрес, но код развёртывается только при первом использовании — центральная идея абстракции аккаунтов) и **детерминированных фабрик** (известный адрес для контракта в любой сети). Острый угол: раз можно поместить код по выбранному адресу, а `SELFDESTRUCT` раньше освобождал этот адрес, контракты могли быть **метаморфными** — разворачивать *другой* код по *тому же* адресу, ломая допущение, что аудированный адрес остаётся тем же, чем был. ([EIP-6780](https://eips.ethereum.org/EIPS/eip-6780) позже обезвредил `SELFDESTRUCT`, во многом закрыв этот трюк.) Опкод-сноска, который незаметно изменил то, как Ethereum думает об адресах.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-1014: Skinny CREATE2", note: "Спецификация — формула адреса и опкод", href: "https://eips.ethereum.org/EIPS/eip-1014" },
    { kind: "post", label: "Глава 1 — аккаунты и адреса", note: "Как адреса выводятся изначально", href: "/you-could-have-invented-ethereum#step-2" },
    { kind: "post", label: "EIP-4337, выведенный", note: "Контрфактические адреса смарт-аккаунтов на практике", href: "/eip-4337-account-abstraction" },
    { kind: "eip", label: "EIP-6780: SELFDESTRUCT restriction", note: "Изменение, закрывшее трюк с метаморфными контрактами", href: "https://eips.ethereum.org/EIPS/eip-6780" },
    { kind: "tool", label: "Калькулятор адреса CREATE2", note: "Вычислить адрес по отправителю, соли и init-коду", href: "https://www.evm.codes/" },
  ]}
/>

</DepthLayer>
