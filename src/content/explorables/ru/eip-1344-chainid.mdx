---
title: "EIP-1344: CHAINID — защита от повтора для подписей контрактов"
dek: "EIP-155 привязал транзакции к их сети. Но контракты всё чаще верифицируют собственные подписанные сообщения — безгазовые одобрения, мета-транзакции, офчейн-ордера — а у них не было никакой привязки к сети, поэтому подпись, действительная для контракта в одной сети, повторно проходила в другой. CHAINID открывает EVM доступ к id текущей сети, так что контракт может привязать свои подписи к сети, на которой он реально работает."
summary: "Пошаговая деривация EIP-1344 (опкод CHAINID), вошедшего в состав Istanbul (декабрь 2019). Почему EIP-155 защищал только транзакции, оставляя верифицируемые контрактами подписи (типизированные данные EIP-712, permit, мета-транзакции) уязвимыми для повтора между сетями и форками, использующими один и тот же адрес контракта; почему жёсткое зашивание id сети на момент деплоя не работает при расщеплении сети (константа неизменно переживает на обеих сторонах); и как CHAINID (0x46) возвращает id текущей сети во время выполнения, так что контракт может встроить его в свой разделитель домена EIP-712 — привязывая каждую подпись к сети, для которой она была подписана, с оговоркой, что закешированный разделитель домена нужно пересчитывать при изменении CHAINID."
topic: EIPs
order: 20
spine: true
prerequisites: ["защита от повтора EIP-155", "подписи и ecrecover"]
updated: 2026-06-24
readingMinutes: 9
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

**Стамбул, декабрь 2019 года.** [EIP-155](/eip-155-replay-protection) решил проблему межсетевого повтора для *транзакций*, встроив `chainId` в то, что подписывается. Но к 2019 году внутри контрактов вырос целый мир подписей — безгазовые одобрения токенов (`permit`), мета-транзакции, офчейн-ордера DEX — где пользователь подписывает **сообщение**, а контракт верифицирует его через `ecrecover`. У этих подписей была ровно та же проблема, что решил 155, только уровнем выше: никакой привязки к сети, поэтому подпись, действительная для контракта в одной сети, была действительна для того же контракта везде. Загвоздка в том, что контракт не может просто *знать* свою сеть — если только протокол ему не скажет. **EIP-1344** — это опкод, который ему это говорит. Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Подписи без привязки к сети" },
  { id: "step-2", label: "Читаем сеть во время выполнения" },
  { id: "step-3", label: "Переживаем расщепление сети" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Подписи без привязки к сети">

## Проблема: подписи контракта повторяются где угодно

Контракт, принимающий подписанные сообщения, верифицирует их через <KeyTerm term="ecrecover" def="Прекомпайл EVM, восстанавливающий адрес подписавшего по хешу сообщения и подписи (v, r, s). Контракт сравнивает восстановленный адрес с ожидаемым — на этом строятся permit, мета-транзакции и офчейн-ордера." />: восстанавливаем подписавшего из `(hash, v, r, s)` и проверяем, что это тот, кто нужен. Но если хеш, который подписал пользователь, ничего не говорит о том, *в какой сети* работает контракт, то одна и та же подпись восстанавливает того же подписавшего для **того же адреса контракта** в любой сети — а адреса контрактов часто совпадают между сетями (особенно после форка или через [CREATE2](/eip-1014-create2)). Так что `permit`, подписанный вами в mainnet, можно повторно провести против контракта-близнеца в другой сети.

<Scene name="ci-replay" lang="ru" caption="Пользователь подписывает permit; контракт верифицирует его через ecrecover. Поскольку подписанное сообщение не несёт привязки к сети, идентичная подпись принимается для того же адреса контракта в любой сети — межсетевой повтор, та же проблема, что решал EIP-155, только на уровне подписей контракта." />

<Callout type="warning" title="Что ломается">
Нам нужно, чтобы подписанное сообщение фиксировало сеть — так же, как это делает транзакция по EIP-155. Но именно *контракт* формирует сообщение для верификации — а у контракта, работающего в EVM, нет способа спросить «в какой я сети?». Для этого нет опкода. Может, просто зашить id сети на момент деплоя?
</Callout>

**→ Шаг 2: дадим EVM способ читать сеть.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Читаем сеть во время выполнения">

## CHAINID: id текущей сети, во время выполнения

Зашить id на момент деплоя заманчиво, но неверно — почему, увидим через минуту, — так что настоящее решение делает id сети *читаемым во время работы контракта*. <KeyTerm term="CHAINID" def="Опкод 0x46 из Istanbul. Кладёт в стек id сети, в которой исполняется транзакция, — то же значение, что использует EIP-155, — так что код контракта может читать свою сеть во время выполнения." /> (опкод `0x46`) кладёт id текущей сети в стек. Теперь контракт может встроить этот id в <KeyTerm term="разделитель домена" def="Значение EIP-712, ограничивающее подпись конкретным приложением и деплоем: хеширует вместе имя контракта, версию, chainId и адрес верифицирующего контракта. Подписи делаются над (разделителем домена, сообщением), поэтому действительны только в этом точном домене." /> — значение EIP-712, ограничивающее подпись *этим приложением, этим контрактом, этой сетью*. Сообщение, которое подписывает пользователь, составляется поверх этого домена, поэтому подпись действительна только там, где домен совпадает.

<Scene name="ci-domain" lang="ru" caption="CHAINID (0x46) возвращает id сети, в которой выполняется код. Контракт встраивает его в свой разделитель домена EIP-712 (chainId + верифицирующий контракт), а сообщение пользователя подписывается поверх этого домена — так что подпись действительна только в сети 1 и только для этого контракта." />

<Formula
  lang="ru"
  expr="domainSeparator = keccak256( name, version, CHAINID, address(this) )"
  terms={[
    { t: "CHAINID", note: "читается во время выполнения (0x46) — поэтому отражает сеть, в которой контракт реально работает" },
    { t: "address(this)", note: "ограничивает именно этим контрактом, а не только этой сетью" },
  ]}
  caption="Подпишите поверх этого домена — и подпись намертво сварена с (этой сетью, этим контрактом) — версия трюка EIP-155 на уровне контракта."
/>

<SpecExcerpt lang="ru"
  source="EIP-1344: ChainID opcode"
  href="https://eips.ethereum.org/EIPS/eip-1344"
  ref="опкод"
>
Добавляет `CHAINID` (`0x46`), возвращающий `chain_id` текущей сети, как он определён в
EIP-155 — значение, фиксированное для каждой сети. Он позволяет контрактам включать id
сети в подписываемые данные (например, в разделитель домена EIP-712), чтобы подписи,
сделанные для одной сети, не были действительны в другой. Замечание: читать его нужно
именно во время выполнения, чтобы значение оставалось верным после хардфорка, меняющего
id сети.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Так почему бы просто не прочитать `CHAINID` один раз при деплое и не сохранить как константу? На обычной сети ничего не сломается — но весь смысл id сети в том, чтобы различать сети *после их расщепления*, а сохранённая константа на это неспособна.
</Callout>

**→ Шаг 3: расщепление — вот в чём весь смысл.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Переживаем расщепление сети">

## Почему это нужно читать «вживую»: расщепление сети

Представьте хардфорк, расщепляющий одну сеть на две — ровно тот сценарий, из которого родился EIP-155. Ваш контракт теперь существует, байт в байт, на **обеих** сетях по одному и тому же адресу. Если бы он *зашил* id сети на момент деплоя, обе копии несли бы **одну и ту же** константу, и подпись по-прежнему проходила бы на обеих — дыра никогда не закроется. Поскольку `CHAINID` читается **во время выполнения**, две копии теперь возвращают **разные** id: в исходной сети это `1`, в новой — скажем, `61`. Разделитель домена отличается, поэтому подпись, сделанная для сети `1`, при пересчёте даёт домен, не совпадающий с сетью `61`, и отклоняется. Защита автоматически отслеживает расщепление.

<Scene name="ci-split" lang="ru" caption="После расщепления один и тот же контракт работает в обеих сетях, но CHAINID возвращает в каждой разный id. Подпись, сделанная для сети 1, совпадает с доменом там (✓), но не в сети 61, где домен теперь использует 61 (✗). Оговорка: контракт, закешировавший разделитель домена при деплое, должен пересчитать его при изменении CHAINID — иначе кеш заново откроет дыру." />

<Callout type="insight" title="Вот и весь CHAINID">
EIP-155 защищал **транзакции**, но верифицируемые контрактом **сообщения** (permit, мета-tx, офчейн-ордера) не имели привязки к сети и потому повторялись против того же контракта в любой сети → решение — дать контракту читать свою сеть, но **зашивание** её константой не переживает расщепление (константа выживает на обеих сторонах) → поэтому `CHAINID` (0x46) возвращает id **во время выполнения**, а контракт встраивает его в свой **разделитель домена EIP-712**, намертво сваривая каждую подпись с сетью, для которой она была подписана. Это идея EIP-155, поднятая из протокола в EVM, и именно она делает `permit`, безгазовые одобрения и любую подпись EIP-712 безопасными между сетями и форками — с одной острой оговоркой: *закешированный* разделитель домена нужно инвалидировать при изменении id сети. Читайте сеть вживую — или кеш соврёт.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-1344: ChainID opcode", note: "Спецификация — 0x46 и оговорка про расщепление", href: "https://eips.ethereum.org/EIPS/eip-1344" },
    { kind: "post", label: "EIP-155, выведенный", note: "Защита от повтора на уровне транзакций, которую это расширяет", href: "/eip-155-replay-protection" },
    { kind: "eip", label: "EIP-712: Typed structured data signing", note: "Разделитель домена, который питает CHAINID", href: "https://eips.ethereum.org/EIPS/eip-712" },
    { kind: "eip", label: "EIP-2612: permit", note: "Безгазовые одобрения — главный пользователь CHAINID", href: "https://eips.ethereum.org/EIPS/eip-2612" },
    { kind: "post", label: "EIP-1014 — CREATE2", note: "Почему один и тот же адрес контракта повторяется в разных сетях", href: "/eip-1014-create2" },
  ]}
/>

</DepthLayer>
