---
title: "EIP-145: даём 256-битной машине инструкцию сдвига"
dek: "У EVM с первого дня были AND, OR, XOR и NOT — но не было битового сдвига. Контракты имитировали сдвиг умножением и делением на степени двойки: дороже, громоздче и неспособно корректно сдвигать знаковые числа. EIP-145 добавляет нативные SHL, SHR и SAR."
summary: "Пошаговая деривация EIP-145 (инструкций битового сдвига), выпущенного в форке Constantinople (февраль 2019). Почему словоориентированная 256-битная VM, вышедшая без опкодов сдвига, была реальным пробелом — битовый сдвиг лежит в основе упаковки полей структур, работы с битовыми картами, арифметики с фиксированной точкой и криптографии, а контрактам приходилось эмулировать сдвиг влево как умножение на 2ⁿ, а сдвиг вправо как деление на 2ⁿ, платя за MUL/DIV плюс проталкивание константы 2ⁿ и всё равно не имея возможности выразить корректный арифметический (сохраняющий знак) сдвиг вправо, потому что DIV округляет к нулю; и как EIP-145 добавляет три нативных опкода — SHL (0x1b), SHR (логический, 0x1c) и SAR (арифметический, 0x1d), — каждый стоит базовых 3 газа и сдвигает напрямую без проталкивания константы; выигрыш — более дешёвый и компактный байткод для всей работы с битами, на которую опираются упаковка и криптография, корректный знаковый сдвиг через SAR и, поскольку компиляторы генерируют их автоматически, более компактные контракты бесплатно."
topic: EIPs
order: 38
spine: true
prerequisites: ["словные и битовые операции EVM"]
updated: 2026-07-02
readingMinutes: 6
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**Constantinople, февраль 2019.** [EVM работает со 256-битными словами](/the-evm) и с генезиса имеет привычные битовые операторы — `AND`, `OR`, `XOR`, `NOT`. Но не хватало того, к которому любой ассемблерный программист потянулся бы немедленно: **битового сдвига**. Сдвиг битов влево или вправо — это способ упаковать несколько полей в одно слово, обойти битовую карту, сделать арифметику с фиксированной точкой или реализовать хеш. На машине, построенной вокруг 256-битных слов, его отсутствие бросается в глаза — а обходным решением было привлечь на помощь арифметику. **EIP-145** наконец даёт машине ту инструкцию, которая должна была быть у неё с самого начала. Давайте выведем её.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Сдвиг арифметикой" },
  { id: "step-2", label: "SHL, SHR, SAR" },
  { id: "step-3", label: "Компактнее работа с битами" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Сдвиг арифметикой">

## Проблема: нет сдвига, значит — умножаем и делим

Без опкода сдвига контракты опирались на тождество теории чисел: сдвиг влево на `n` битов — то же самое, что умножение на 2ⁿ, а сдвиг вправо на `n` — деление на 2ⁿ. Так что сдвиг влево превращался в `MUL`, а сдвиг вправо — в `DIV`, причём каждый из них требовал сначала протолкнуть на стек константу 2ⁿ, добавляя байткод и газ. Это работает для беззнаковых значений, но это долгий обходной путь для того, что CPU делает за один такт. Хуже того, не было чистого способа выразить <KeyTerm term="arithmetic right shift" def="Сдвиг вправо, сохраняющий знаковый бит, так что сдвиг отрицательного числа вправо оставляет его отрицательным (эквивалент деления на степень двойки с округлением вниз). DIV округляет к нулю, а не в сторону минус бесконечности, поэтому не может воспроизвести корректный знаковый сдвиг — знак приходилось обрабатывать отдельным случаем вручную." /> (арифметический сдвиг вправо): `DIV` округляет к нулю, а не в сторону минус бесконечности, так что сдвиг знакового (отрицательного) числа вправо давал неверный результат, если только вы сами не обрабатывали знак отдельным случаем.

<Scene name="sh-emul" lang="ru" caption="У EVM были AND/OR/XOR/NOT, но не было сдвига, так что контракты эмулировали сдвиг влево как MUL на 2ⁿ, а сдвиг вправо как DIV на 2ⁿ — платя за умножение/деление плюс проталкивание константы. А DIV не может выразить корректный арифметический (сохраняющий знак) сдвиг вправо. Базовая операция CPU, симулируемая арифметикой." />

<Callout type="warning" title="Что ломается">
Каждый битовый сдвиг стоит умножения или деления плюс проталкивания константы — на операциях, которые упаковочный код и криптография выполняют постоянно, — а знаковые сдвиги вправо требуют неуклюжей, подверженной ошибкам обработки знака. Словоориентированная VM должна сдвигать биты нативно.
</Callout>

**→ Шаг 2: добавляем все три сдвига напрямую.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="SHL, SHR, SAR">

## EIP-145: три нативных опкода сдвига

Исправление добавляет ровно те три сдвига, которые нужны знаковой/беззнаковой машине. <KeyTerm term="SHL" def="Shift Left (0x1b): снимает со стека величину сдвига и значение, сдвигает значение влево на это число битов, заполняя нулями. Эквивалент старого MUL на 2ⁿ, но одна нативная инструкция за 3 газа." /> (`0x1b`) сдвигает влево; <KeyTerm term="SHR" def="Logical Shift Right (0x1c): сдвигает вправо, заполняя верх нулями — беззнаковый сдвиг вправо, эквивалент DIV на 2ⁿ для неотрицательных значений." /> (`0x1c`) — это логический (заполняющий нулями) сдвиг вправо; а <KeyTerm term="SAR" def="Arithmetic Shift Right (0x1d): сдвигает вправо, сохраняя знаковый бит, так что отрицательные числа остаются отрицательными (семантика деления с округлением вниз). Это та операция, которую DIV никогда не мог выразить чисто." /> (`0x1d`) — это арифметический (сохраняющий знак) сдвиг вправо, который старый трюк с `DIV` не мог сделать. Каждый снимает со стека величину сдвига и значение, сдвигает напрямую и стоит базовых **3 газа**, как и другие битовые операции, — никаких `MUL`/`DIV`, никакой константы для проталкивания.

<Scene name="sh-ops" lang="ru" caption="EIP-145 добавляет SHL (влево), SHR (логический вправо) и SAR (арифметический, сохраняющий знак, вправо). Каждый снимает со стека величину и значение и сдвигает напрямую за 3 газа, без проталкивания константы — а SAR наконец даёт корректный знаковый сдвиг вправо." />

<SpecExcerpt
  lang="ru"
  source="EIP-145: Bitwise shifting instructions in EVM"
  href="https://eips.ethereum.org/EIPS/eip-145"
  ref="опкоды"
>
Добавляет SHL (0x1b), SHR (0x1c) и SAR (0x1d): логический сдвиг влево, логический сдвиг
вправо и арифметический (сохраняющий знак) сдвиг вправо. Каждый снимает со стека величину
сдвига и значение и стоит стандартных 3 газа, устраняя необходимость эмулировать сдвиги
через MUL и DIV.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Ничего — изменение чисто аддитивное. Существующие сдвиги на основе MUL/DIV продолжают работать, они просто становятся ненужными. Ценность в том, что рутинная операция перестаёт стоить газа арифметической операции и обретает корректный знаковый вариант.
</Callout>

**→ Шаг 3: всё, что сдвигает биты, становится компактнее.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Компактнее работа с битами">

## Выигрыш: дешевле, компактнее и корректно

Битовый сдвиг повсюду под поверхностью — упаковка нескольких значений в один 256-битный слот, чтение и запись битовых карт, арифметика с фиксированной точкой и особенно криптографические процедуры, перемешивающие биты тысячами. Превращение его в одну инструкцию за 3 газа (вместо `MUL`/`DIV` плюс проталкивание константы) урезает газ и байткод во всём этом, а `SAR` устраняет целый класс ошибок знакового сдвига. Как и с каждым EIP типа «недостающий примитив» — [PUSH0](/eip-3855-push0), [MCOPY](/eip-5656-mcopy), — выигрыш тихий и автоматический: компиляторы генерируют новые опкоды там, где раньше генерировали арифметический обходной путь, так что контракты становятся дешевле и компактнее просто от перекомпиляции.

<Scene name="sh-result" lang="ru" caption="Нативные сдвиги делают упаковку, битовые карты, арифметику с фиксированной точкой и криптографию дешевле и компактнее и дают корректный знаковый сдвиг через SAR. Компиляторы генерируют их автоматически — та же история «добавь недостающий примитив», что у PUSH0 и MCOPY." />

<Callout type="insight" title="Вот что такое EIP-145">
256-битная, словоориентированная VM вышла с `AND`/`OR`/`XOR`/`NOT`, но **без сдвига**, так что контракты эмулировали сдвиг влево как **MUL на 2ⁿ**, а сдвиг вправо как **DIV на 2ⁿ** — лишний газ, проталкивание константы и никакого корректного знакового сдвига → EIP-145 добавляет **SHL**, **SHR** и **SAR** как нативные опкоды за 3 газа, сдвигающие напрямую, причём `SAR` сохраняет знак → так что весь код упаковки битов, битовых карт, фиксированной точки и криптографии становится **дешевле, компактнее и корректнее** — автоматически, при перекомпиляции. Это EIP наименьшего рода — три опкода, которые у CPU были всегда, — и напоминание о том, что даже машину, спроектированную вокруг 256-битных слов, пришлось *научить* сдвигать эти биты вбок.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-145: Bitwise shifting instructions", note: "Спецификация — SHL, SHR, SAR", href: "https://eips.ethereum.org/EIPS/eip-145" },
    { kind: "post", label: "EIP-3855 — PUSH0", note: "Ещё один опкод «добавь недостающий примитив»", href: "/eip-3855-push0" },
    { kind: "post", label: "EIP-5656 — MCOPY", note: "Примитив копирования памяти с той же историей", href: "/eip-5656-mcopy" },
    { kind: "post", label: "Глава 2 — EVM", note: "256-битное слово, над которым работают эти сдвиги", href: "/the-evm" },
  ]}
/>

</DepthLayer>
