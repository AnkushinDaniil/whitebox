---
title: "EIP-155: привязываем подпись к своей цепочке"
dek: "Подпись Ethereum до EIP-155 говорила, кто подписал и что подписал — но никогда не говорила, для какой сети это было. Так что в точности одни и те же подписанные байты были валидны на каждой цепочке, разделяющей ваш адрес, и транзакцию можно было воспроизвести через форк вроде ETH/ETC. EIP-155 вплетает chainId в то, что вы подписываете, так что подпись оказывается заперта на одной цепочке."
summary: "Пошаговая деривация EIP-155 (простая защита от атак воспроизведения) — почему ранняя подпись Ethereum не несла идентичность цепочки, из-за чего одна и та же подписанная транзакция была валидна на любой цепочке, разделяющей ваш аккаунт, и могла быть воспроизведена через раскол цепочки, и как работает исправление: включить chainId (с двумя нулевыми заглушками) в RLP, который хешируется и подписывается, и закодировать его в v как chainId·2 + 35 + чётность, так что подпись, сделанная для одной цепочки, даёт неверный хеш — и провальное восстановление подписанта — где угодно ещё, в то время как унаследованные транзакции с v=27/28 остаются валидными, но без защиты."
topic: EIPs
order: 9
spine: true
prerequisites: ["цифровые подписи", "что такое транзакция"]
updated: 2026-06-23
readingMinutes: 10
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";

Подпись доказывает две вещи: *кто* подписал и *что* он подписал. Ранние транзакции Ethereum подписывались над `[nonce, gasPrice, gasLimit, to, value, data]` — действие, но **не сеть, для которой оно предназначалось**. Это упущение кажется безобидным, пока не вспомнить, что ваш закрытый ключ выводит *один и тот же адрес на каждой Ethereum-подобной цепочке*. Так что идеально валидная подписанная транзакция на одной сети — это идеально валидная подписанная транзакция и на другой: скопируйте байты, отправьте их в другом месте, и они выполнятся снова. Это обернулось катастрофой при расколе ETH/ETC в 2016 году, когда две цепочки внезапно стали делить адреса, балансы и nonce. **EIP-155** — это идея в одну строку, которая это чинит: поместить цепочку *внутрь подписи*. Давайте выведем её.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Подпись без цепочки" },
  { id: "step-2", label: "Подписываем и chainId" },
  { id: "step-3", label: "Несём его в v" },
  { id: "step-4", label: "Не та цепочка — отклонено" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Подпись без цепочки">

## Проблема: подпись, валидная везде

Чтобы отправить транзакцию, вы подписываете хеш её полей. До EIP-155 этими полями были просто `[nonce, gasPrice, gasLimit, to, value, data]`, а подпись `(v, r, s)` использовала `v = 27` или `28` — чисто <KeyTerm term="идентификатор восстановления" def="Дополнительный бит (чётность), хранимый в v, который позволяет проверяющему восстановить открытый ключ подписанта из r и s. До EIP-155 это было просто 27 или 28 — один бит, не несущий больше никакой информации." /> (recovery id), сообщающий проверяющему, какой из двух возможных открытых ключей был восстановлен. Нигде в подписанных данных нет ничего, что говорило бы: *«эта транзакция для мейннета Ethereum»*. Так что подпись авторизует действие на **любой** цепочке, которая распознаёт ваш адрес.

<Scene name="r155-replay" lang="ru" caption="Подпись до EIP-155 покрывает действие, но не сеть. Поскольку ваш ключ даёт вам один и тот же адрес на каждой цепочке, идентичные подписанные байты валидны на всех них — так что транзакцию, разосланную в одной цепочке, можно скопировать и воспроизвести в другой." />

Это <KeyTerm term="атака воспроизведения" def="Повторная рассылка валидно подписанной транзакции в другой цепочке (или после форка), где подписант никогда её не предполагал. Поскольку подпись всё ещё валидна, транзакция выполняется снова, перемещая средства, которые пользователь никогда не собирался туда перемещать." /> (replay attack). После хардфорка DAO у ETH и ETC была идентичная история вплоть до раскола, так что транзакция, сделанная вами в одной цепочке, мгновенно становилась воспроизводимой в другой — опустошая или дублируя переводы без какого-либо дополнительного подписания.

<Callout type="warning" title="Что ломается">
Подпись должна фиксировать, *для какой цепочки* она сделана, чтобы её нельзя было перенести на другую. Но куда нам поместить идентичность цепочки — и как, не сломав каждую транзакцию, уже подписанную старым способом?
</Callout>

**→ Шаг 2: делаем цепочку частью того, что вы подписываете.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Подписываем и chainId">

## Вплетаем chainId в подписанные данные

Дайте каждой сети публичный номер — <KeyTerm term="chainId" def="Уникальное целое число, идентифицирующее цепочку: 1 для мейннета Ethereum, 11155111 для Sepolia, 61 для Ethereum Classic и так далее. Зарегистрировано, чтобы кошельки и узлы соглашались, для какой сети они подписывают." /> (мейннет — это `1`, Ethereum Classic — `61`). Теперь, вместо хеширования всего шести полей, хешируем список **с добавленным chainId** плюс две нулевые заглушки: `[nonce, gasPrice, gasLimit, to, value, data, chainId, 0, 0]`. Именно весь этот список keccak-хешируется и подписывается. Поскольку chainId теперь *внутри* хеша, подписываемый дайджест отличается на каждой цепочке — подпись, вычисленная для chainId `1`, просто не является подписью над дайджестом, который вычислил бы chainId `61`.

<Scene name="r155-bind" lang="ru" caption="Добавьте chainId (и две нулевые заглушки) к полям перед хешированием. chainId теперь часть подписываемого дайджеста, так что одна и та же транзакция даёт разный хеш — и, значит, требует разной подписи — на каждой цепочке." />

Два хвостовых нуля — изящный трюк: они делают подписываемую структуру похожей на девятиэлементную транзакцию, чьи слоты `(v, r, s)` равны `(chainId, 0, 0)`, переиспользуя уже существующую форму кодирования вместо изобретения новой.

<Callout type="warning" title="Что ломается">
Подписанный хеш теперь зависит от цепочки — хорошо. Но сама *транзакция* всё ещё несёт только `(v, r, s)`. Как получатель узнаёт, какой chainId подставить обратно при восстановлении подписанта, и как продолжают работать старые транзакции с `v = 27/28`?
</Callout>

**→ Шаг 3: протаскиваем chainId внутрь v.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Несём его в v">

## Кодируем chainId в v — с обратной совместимостью

Для нового поля нет места, так что переиспользуем `v`. Переопределим его как `v = chainId·2 + 35 + parity`, где `parity` — это бит восстановления `0`/`1`. На мейннете (chainId `1`) это даёт `v = 37` или `38`; получатель читает `v`, извлекает из него chainId и точно знает, какой дайджест восстановить перед восстановлением подписанта. И, что критично, любое унаследованное значение *ниже* этого диапазона: `v = 27/28` по-прежнему означает «без защиты, без chainId», так что старые транзакции остаются валидными навсегда — они просто воспроизводимы, по собственному выбору.

<Scene name="r155-vencode" lang="ru" caption="chainId едет внутри v как chainId·2 + 35 + parity (так что chainId 1 → v равен 37 или 38). Получатель восстанавливает chainId из v; унаследованные транзакции с v = 27/28 по-прежнему валидируются, просто без защиты от воспроизведения." />

<SpecExcerpt
  lang="ru"
  source="EIP-155: Simple replay attack protection"
  href="https://eips.ethereum.org/EIPS/eip-155"
  ref="схема подписания"
>
Если `block.number ≥ FORK_BLKNUM`, подписывается девятиэлементный RLP
`[nonce, gasprice, gaslimit, to, value, data, chainid, 0, 0]` вместо шестиэлементного
списка. Тогда `v = chain_id · 2 + 35` или `chain_id · 2 + 36` (две чётности). У
мейннета `chain_id = 1`, так что `v ∈ {37, 38}`. Транзакции, всё ещё подписанные
старым способом (`v ∈ {27, 28}`), остаются валидными, но не предлагают защиты от
воспроизведения.
</SpecExcerpt>

<Formula
  lang="ru"
  expr="v = chainId · 2 + 35 + parity"
  terms={[
    { t: "chainId", note: "идентификатор сети — 1 для мейннета, 61 для Ethereum Classic" },
    { t: "35", note: "смещение EIP-155; сохраняет валидность унаследованных v = 27/28" },
    { t: "parity", note: "бит восстановления подписи, 0 или 1" },
  ]}
  caption="Цепочка вварена в v, так что подпись восстанавливает верного подписанта только на своей собственной цепочке."
/>

<Callout type="warning" title="Что ломается">
Итак, chainId подписан и несётся вместе с транзакцией. Что на самом деле *останавливает* воспроизведение, когда кто-то отправляет транзакцию с chainId-1 в цепочке с chainId 61?
</Callout>

**→ Шаг 4: смотрим, как не та цепочка её отклоняет.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Не та цепочка — отклонено">

## Награда: воспроизведение проваливает восстановление подписанта

Теперь проследите за атакующим, копирующим вашу транзакцию с chainId-`1` в цепочку с chainId `61`. Эта цепочка валидирует, восстанавливая подписанный дайджест **со своим собственным chainId, 61** — не 1. Хеш, который она строит, отличается от того, который вы действительно подписали, так что когда она запускает ECDSA-восстановление над вашими `(r, s)`, выскакивающий открытый ключ — **не ваш адрес**. Транзакция выглядит подписанной каким-то случайным аккаунтом без средств, и она отклоняется. Подпись была криптографически вварена в цепочку 1; она не может путешествовать.

<Scene name="r155-reject" lang="ru" caption="При воспроизведении в chainId 61 цепочка перестраивает дайджест со своим собственным id, так что восстановление подписанта возвращает неверный адрес — не отправителя. Подпись совпадает только с той цепочкой, для которой она сделана, так что воспроизведение отклоняется." />

<Callout type="insight" title="И это и есть EIP-155">
Подпись до EIP-155 фиксировала действие, но не сеть → так что одни и те же подписанные байты воспроизводились в любой цепочке, разделяющей ваш адрес (особенно остро — при расколе ETH/ETC) → вплетите привязанный к сети **chainId** в RLP, который хешируется и подписывается → несите его в **v = chainId·2 + 35 + parity**, который одновременно транспортирует chainId и остаётся выше унаследованного диапазона `27/28` ради обратной совместимости → и теперь транзакция, подписанная для одной цепочки, восстанавливает *неверного* подписанта где угодно ещё, так что она отклоняется. Одно целое число, вмешанное в хеш, превращает подпись из «валидна везде» в «валидна здесь». Именно поэтому ваш кошелёк спрашивает, в какой вы сети — и именно поэтому транзакция мейннета не может тихо опустошить ваш баланс в тестнете (или в Classic).
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-155: Simple replay attack protection", note: "Спецификация — chainId в подписи и в v", href: "https://eips.ethereum.org/EIPS/eip-155" },
    { kind: "post", label: "Keys, signatures & addresses", note: "Почему один ключ — это один и тот же адрес на каждой цепочке", href: "/you-could-have-invented-ethereum#step-2" },
    { kind: "post", label: "EIP-2718, derived", note: "Типизированный конверт, который позже несёт chainId явно", href: "/eip-2718-typed-transactions" },
    { kind: "tool", label: "chainlist.org", note: "Реестр используемых chainId", href: "https://chainlist.org/" },
    { kind: "post", label: "The DAO fork & ETH/ETC split", note: "Событие, сделавшее защиту от воспроизведения неотложной", href: "https://ethereum.org/en/history/#dao-fork" },
  ]}
/>

</DepthLayer>
