---
title: "EIP-4788: учим EVM доверять beacon-цепочке"
dek: "После The Merge консенсус-слой хранит ровно те данные, которые нужны контрактам стейкинга и рестейкинга, — балансы валидаторов, статусы, слэшинги, — но EVM не видит из этого ничего, и проекты опирались на доверенные оракулы. EIP-4788 раскрывает корень родительского beacon-блока внутри EVM, так что контракт может проверить любой факт консенсуса доказательством Меркла — и без оракула."
summary: "Пошаговая деривация EIP-4788 (корень beacon-блока в EVM) — почему у слоя исполнения не было доступа к состоянию консенсус-слоя с минимизированным доверием (балансы валидаторов, статусы, слэшинги, credentials для вывода, RANDAO), что вынуждало протоколы ликвидного стейкинга и рестейкинга опираться на доверенные оракулы, и как работает исправление: не нужно всё состояние — нужен лишь подлинный коммитмент к нему — корень beacon-блока это hash-tree-root по всему состоянию beacon-цепочки, — поэтому протокол штампует корень родительского beacon-блока в EVM каждый блок через новое поле заголовка parentBeaconBlockRoot и системный вызов, записывающий пару (timestamp → root) в предразвёрнутый контракт-кольцевой буфер «beacon roots» (~8191 слотов, ~27 часов); затем контракт вызывает этот предеплой с timestamp, чтобы получить подлинный корень, и проверяет доказательство Меркла любого факта о валидаторе против него."
topic: EIPs
order: 13
spine: true
prerequisites: ["The Merge (два слоя)", "доказательства Меркла"]
updated: 2026-06-23
readingMinutes: 11
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";
import MathViz from "@/components/explorable/MathViz.astro";

После The Merge самые интересные данные Ethereum — кто валидатор, его баланс, был ли он слэшнут, его credentials для вывода — живут на **консенсус-слое**, а **слой исполнения** (EVM, где выполняется каждый смарт-контракт) не видит из этого ни байта. При этом именно на этих данных построены ликвидный стейкинг (Lido), рестейкинг (EigenLayer) и протоколы валидаторских доказательств. Не имея нативного доступа, они откатывались к **доверенным оракулам**: комитет публиковал факты консенсуса ончейн, и вам приходилось просто доверять. **EIP-4788** убирает комитет. Давайте выведем, как.

<JourneyNav client:load steps={[
  { id: "step-1", label: "EVM слеп к консенсус-слою" },
  { id: "step-2", label: "Коммитмент, а не копия" },
  { id: "step-3", label: "Штампуем корень каждый блок" },
  { id: "step-4", label: "Читаем его, доказываем что угодно" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="EVM слеп к консенсус-слою">

## Проблема: состояние консенсуса прямо здесь — и недостижимо

Узел после The Merge исполняет два слоя, но смарт-контракт выполняется целиком внутри мирового состояния слоя исполнения. <KeyTerm term="состояние beacon-цепочки (beacon state)" def="Состояние консенсус-слоя: полный реестр валидаторов — баланс, статус активации/выхода, флаг слэшинга, credentials для вывода каждого валидатора, — плюс RANDAO и прочее. Именно на этом работает proof of stake, и это невидимо для EVM." /> находится в другом слое, и нет ни опкода, ни прекомпайла, ни поля, чтобы до него дотянуться. Так что контракт стейкинга, желающий узнать, *«держит ли валидатор N всё ещё 32 ETH, и не слэшнут ли он»*, не имеет доверенного способа это выяснить. Единственным вариантом был <KeyTerm term="оракул (oracle)" def="Офчейн-комитет (часто мультиподпись), который читает состояние консенсуса и публикует его в контракт слоя исполнения. Пользователям приходится доверять, что он честен и жив, — центральная точка отказа, надстроенная над децентрализованной цепочкой." />: какая-то доверенная сторона следит за beacon-цепочкой и записывает ответ в контракт.

<Scene name="br-blind" lang="ru" caption="Балансы, статусы, слэшинги и credentials валидаторов живут на консенсус-слое; контракт слоя исполнения не может прочитать ничего из этого. Единственным мостом был доверенный оракул, публикующий данные, — центральная точка отказа в системе, которая иначе не требует доверия." />

Именно такое предположение о доверии меньше всего хочется иметь под миллиардами застейканных ETH. Если оракул лжёт или уходит в офлайн, построенный на нём протокол ломается.

<Callout type="warning" title="Что ломается">
Нам нужно, чтобы контракты узнавали факты консенсуса *без* доверия комитету. Но копировать всё состояние beacon-цепочки в EVM каждый блок абсурдно дорого. Есть ли что-то маленькое, что протокол мог бы раскрыть, но что всё ещё позволяет проверить что угодно?
</Callout>

**→ Шаг 2: раскрываем коммитмент, а не данные.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Коммитмент, а не копия">

## Вам не нужно состояние — вам нужен его корень

Beacon-цепочка уже хеширует всё своё состояние в единый 32-байтовый коммитмент: <KeyTerm term="корень beacon-блока (beacon block root)" def="Hash-tree-root beacon-блока, который рекурсивно коммитит (через SSZ-меркилизацию) ко всему состоянию beacon-цепочки. Любой отдельный факт — баланс конкретного валидатора — это лист под этим корнем, доказуемый веткой Меркла." />. Поскольку этот корень — корень Меркла по всему состоянию beacon-цепочки, любой отдельный факт — баланс одного валидатора, его статус слэшинга — это лист, который можно доказать веткой Меркла. Так что контракту, держащему *подлинный* недавний beacon-корень, не нужно, чтобы состояние копировали внутрь: кто угодно может вручить ему доказательство Меркла, а контракт проверит доказательство против корня, которому доверяет. Дорогое глобальное состояние схлопывается до одного хеша плюс короткое доказательство на запрос.

<Scene name="br-commit" lang="ru" caption="Корень beacon-блока — это корень Меркла по всему состоянию beacon-цепочки, так что каждый факт — лист под ним. Имея подлинный корень, короткое доказательство Меркла верифицирует любое отдельное значение — баланс, статус или credentials валидатора — без копирования состояния." />

Это переформулирует всю задачу. Протоколу не нужно *отдавать* состояние beacon-цепочки в EVM — ему нужно лишь сделать доступной и заслуживающей доверия одну вещь: недавний, подлинный корень beacon-блока.

<Callout type="warning" title="Что ломается">
Доказательство Меркла настолько заслуживает доверия, насколько заслуживает доверия корень, против которого его проверяют. Если корень предоставляет *пользователь*, он может солгать. Кто помещает подлинный корень в EVM, и как контракту узнать, что он настоящий?
</Callout>

**→ Шаг 3: пусть сам протокол записывает корень.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Штампуем корень каждый блок">

## Протокол штампует корень родительского beacon-блока каждый блок

Корень должен помещать протокол, а не пользователь. Поэтому EIP-4788 добавляет поле `parentBeaconBlockRoot` в заголовок блока **исполнения** (каждый блок исполнения уже соответствует beacon-блоку, так что знает корень своего родителя), и в самом начале обработки каждого блока **системный вызов** — от специального системного адреса `0xfff…ffe`, который никто не может подделать, — записывает пару `(timestamp → parentBeaconBlockRoot)` в предразвёрнутый <KeyTerm term="контракт beacon-корней (beacon roots contract)" def="Предразвёрнутый системный контракт по адресу 0x000F3df6…beac02, хранящий кольцевой буфер недавних пар (timestamp → beacon-корень) (~8191 слотов, около 27 часов). Протокол пишет в него каждый блок; контракты читают его, вызывая с timestamp." />. Этот контракт — кольцевой буфер из ~8191 недавних слотов (около 27 часов), так что самые свежие корни всегда доступны для запроса, а старые вытесняются.

<Scene name="br-stamp" lang="ru" caption="Каждый блок добавляет parentBeaconBlockRoot в заголовок, а системный вызов (от 0xff…fe, неподделываемый) записывает запись (timestamp → root) в предеплой beacon-корней — кольцевой буфер из ~8191 слотов (~27ч). Ни пользователь, ни оракул не помещают корень — это делает протокол." />

<SpecExcerpt lang="ru"
  source="EIP-4788: Beacon block root in the EVM"
  href="https://eips.ethereum.org/EIPS/eip-4788"
  ref="поле заголовка и системная запись"
>
Заголовок исполнения получает `parent_beacon_block_root`. В начале каждого блока вызов
от `SYSTEM_ADDRESS` (`0xfff…fffe`) сохраняет `timestamp % 8191 → timestamp` и
`(timestamp % 8191) + 8191 → root` в контракте по адресу `0x000F3df6…beac02`. Вызывающие запрашивают
его, передавая 32-байтовый timestamp в calldata; он возвращает соответствующий корень.
</SpecExcerpt>

<Formula
  lang="ru"
  expr="slot = timestamp mod 8191"
  terms={[
    { t: "timestamp", note: "временная метка блока — ключ, по которому вы запрашиваете предеплой" },
    { t: "8191", note: "размер кольцевого буфера: ~27 часов корней (простое число, чтобы равномерно распределять записи)" },
  ]}
  caption="Корни живут в кольце фиксированного размера, так что старые вытесняются, а ончейн-хранилище остаётся ограниченным."
/>

<MathViz
  lang="ru"
  name="ring-buffer"
  caption="Предеплой — это кольцевой буфер, и именно modulo делает его таковым. Каждый блок записывает (timestamp → root) в слот timestamp mod 8191; указатель записи движется по кругу, и когда возвращается, перезаписывает самую старую запись — так контракт вечно хранит ~27 часов корней в ограниченном хранилище."
/>

<Callout type="warning" title="Что ломается">
Теперь подлинный корень лежит в контракте, который может вызвать кто угодно. Как протокол реально использует его от начала до конца?
</Callout>

**→ Шаг 4: читаем корень, доказываем факт.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Читаем его, доказываем что угодно">

## Выигрыш: подлинный корень, затем доказательство

Теперь любой контракт может сделать полностью ончейн то, для чего раньше требовался оракул. Он **вызывает** предеплой beacon-корней с timestamp и получает обратно подлинный beacon-корень для этого слота. Затем он принимает **доказательство Меркла** — предоставленное тем, кто хочет что-то доказать, — что баланс (или статус, или credential для вывода) конкретного валидатора является листом под этим корнем, и проверяет доказательство. Если оно сходится, факт доказан; если нет — отклонён. Никакого комитета, никакого доверия сверх самого протокола.

<Scene name="br-prove" lang="ru" caption="Контракт вызывает предеплой beacon-корней с timestamp, чтобы получить подлинный корень, затем проверяет доказательство Меркла того, что баланс валидатора лежит под ним. Факт доказан без доверия — оракул исчез." />

<Callout type="insight" title="И это EIP-4788">
EVM не мог видеть состояние консенсуса, поэтому стейкинг и рестейкинг опирались на доверенные оракулы → вам не нужно состояние, нужен лишь подлинный **коммитмент** к нему, а корень beacon-блока — это корень Меркла по всему состоянию → значит сам протокол штампует **корень родительского beacon-блока** в EVM каждый блок (поле заголовка плюс системная запись в предеплой-кольцевой буфер) → и любой контракт читает этот подлинный корень и проверяет против него **доказательство Меркла** любого факта о валидаторе. Предположение о доверии под ликвидным стейкингом, рестейкингом и системами валидаторских доказательств схлопывается из «доверяй этому комитету» в «доверяй протоколу Ethereum, которому ты и так уже доверяешь». Общий приём элегантен: чтобы сделать состояние одного слоя пригодным для использования другим, не отправляйте состояние — отправляйте коммитмент, а доказательствам позвольте сделать остальное.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-4788: Beacon block root in the EVM", note: "Спецификация — поле заголовка, предеплой, кольцевой буфер", href: "https://eips.ethereum.org/EIPS/eip-4788" },
    { kind: "post", label: "The Merge, derived", note: "Почему консенсус и исполнение — разные слои", href: "/eip-3675-the-merge" },
    { kind: "post", label: "EIP-4895, derived", note: "Другой мост консенсус→исполнение: выводы", href: "/eip-4895-withdrawals" },
    { kind: "code", label: "EigenLayer — beacon proofs", note: "Рестейкинг, использующий beacon-корень", href: "https://github.com/Layr-Labs/eigenlayer-contracts" },
    { kind: "tool", label: "SSZ & Merkleization", note: "Как состояние beacon-цепочки хешируется в один корень", href: "https://ethereum.org/en/developers/docs/data-structures-and-encoding/ssz/" },
  ]}
/>

</DepthLayer>
