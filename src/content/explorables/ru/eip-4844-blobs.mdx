---
title: "EIP-4844: блобы — дешёвые данные для роллапов"
dek: "Роллапы обязаны публиковать свои данные на L1, чтобы кто угодно мог их проверить, — но как постоянная calldata это было разорительно дорого. Выведем блобы шаг за шагом: отдельная полоса для данных, коммитмент, чтобы её связать, отсечение через ~18 дней и собственный рынок комиссий."
summary: "Пошаговая деривация EIP-4844 (proto-danksharding) — почему роллапы обязаны публиковать данные ради доступности, почему calldata была неверным местом (постоянна и оценена в той же цене, что исполнение), и как блобы это исправляют: отдельная невидимая для EVM полоса данных, 48-байтовый KZG-коммитмент, связывающий каждый блоб, отсечение через ~18 дней (доступность, а не хранение) и отдельный рынок комиссий blob-газа. Изменение, которое обрушило комиссии L2 в 10–100 раз."
topic: EIPs
order: 5
spine: true
prerequisites: ["роллапы", "газ и EIP-1559"]
updated: 2026-06-23
readingMinutes: 14
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";
import Formula from "@/components/explorable/Formula.astro";
import MathViz from "@/components/explorable/MathViz.astro";

Ethereum масштабируется, выталкивая исполнение на **роллапы** (L2): они прогоняют тысячи транзакций офчейн и публикуют на L1 только результаты. Но роллап обязан опубликовать *данные* этих транзакций где-то публично, иначе никто не смог бы проверить его работу. Годами он публиковал эти данные как обычную **calldata** — и это единственное решение делало комиссии L2 болезненными и ограничивало, насколько дёшево вообще мог стать Ethereum. **EIP-4844** («proto-danksharding») исправил это странно выглядящим новым объектом под названием **блоб**. Странным — пока вы его не построите: начните с проблемы calldata и исправляйте ровно то, что не так, шаг за шагом.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Проблема данных" },
  { id: "step-2", label: "Отдельная полоса" },
  { id: "step-3", label: "Выбрасываем потом" },
  { id: "step-4", label: "Собственный рынок комиссий" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Проблема данных">

## Почему роллапы обязаны публиковать данные — и почему calldata вредила

Роллап сжимает батч транзакций L2 и публикует его на L1. Он *обязан* опубликовать лежащие в основе данные, чтобы кто угодно — пользователь, оспаривающий, конкурирующий узел — мог восстановить состояние L2 и проверить (или опровергнуть) заявленный роллапом результат. Это <KeyTerm term="доступность данных (data availability)" def="Гарантия того, что данные за блоком или батчем действительно опубликованы, так что кто угодно может их скачать и независимо проверить или оспорить состояние. Отличается от хранения их вечно." />: не «хранить их вечно», а «убедиться, что каждый *может* их получить». Очевидным местом для этого была **calldata** транзакции.

<Scene name="blob-calldata" lang="ru" caption="Данные роллапа как calldata: 16 газа/байт, хранятся каждым узлом вечно и оцениваются на том же самом рынке газа, что и ваши свопы, — так что перегрузка L1 и данные роллапа борются за один и тот же блок." />

Три вещи вредят одновременно. Calldata стоит **16 газа за байт** — дорого. Она хранится **каждым полным узлом вечно** — вы платите за постоянное хранение. И она делит **тот же рынок газа**, что и исполнение, так что загруженный L1 напрямую подскакивает стоимость роллапа, а данные роллапа конкурируют за то же место, что и транзакции всех остальных. Комиссии роллапов доминировала эта единственная строка расходов.

<Callout type="warning" title="Что ломается">
Мы платим за то, чтобы хранить *постоянно* то, что нужно лишь *временно*, — роллапам нужно, чтобы их данные были доступны достаточно долго, чтобы их скачали и проверили, а не до конца времён, — и мы оцениваем это на рынке, который никогда не должен был с ним делиться. Оба этих момента неверны.
</Callout>

**→ Шаг 2: дать данным собственную полосу.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Отдельная полоса">

## Отдельная, невидимая для EVM полоса — связанная коммитментом

Добавляем новое место для данных, рядом с calldata, а не внутри неё: **блоб** — большой (~128 КБ) кусок, прикреплённый к новому типу транзакции (`0x03`) в «сайдкаре». Ключевое свойство: **EVM не может прочитать содержимое блоба**. Это не состояние, с которым оперируют контракты, — это просто данные, о публикации которых сеть договорилась. Так что блобы несут данные роллапа, не раздувая состояние, к которому обязан прикасаться EVM.

Но если EVM не может прочитать блоб, а (следующий шаг) узлы собираются его *удалить*, как кто-то потом докажет, что блок действительно включал правильные данные, а не мусор? Свяжите каждый блоб с помощью <KeyTerm term="KZG commitment" def="48-байтовый криптографический коммитмент к блобу (рассматриваемому как полином). Он однозначно фиксирует содержимое блоба и позволяет кому угодно проверить конкретный кусок против него крошечным доказательством — без хранения всего блоба." />: 48-байтового значения, помещённого в блок. EVM получает опкод `BLOBHASH` (версионированный хеш этого коммитмента) и **прекомпайл поточечной проверки (point-evaluation)**, так что контракт роллапа может сверить конкретный кусок блоба с его коммитментом — вообще не храня блоб целиком.

<Scene name="blob-sidecar" lang="ru" caption="Транзакция типа 0x03 несёт блоб в сайдкаре, который EVM не может прочитать. 48-байтовый KZG-коммитмент попадает в заголовок и связывает блоб, так что любой кусок можно верифицировать без полных данных." />

<SpecExcerpt lang="ru"
  source="EIP-4844: Shard Blob Transactions"
  href="https://eips.ethereum.org/EIPS/eip-4844"
  ref="коммитменты блобов"
>
Блоб — это `4096` элементов поля (~128 КБ). Каждый блоб коммитится KZG-
коммитментом; транзакция несёт `versioned_hash = 0x01 ++ sha256(commitment)[1:]`.
EVM никогда не видит байты блоба — только `BLOBHASH(i)` (i-й версионированный хеш) и
прекомпайл поточечной проверки, чтобы верифицировать открытие `(z, y)` против коммитмента.
</SpecExcerpt>

<Formula
  lang="ru"
  expr="versioned_hash = 0x01 ++ sha256(commitment)[1:]"
  terms={[
    { t: "0x01", note: "байт версии — позволяет схеме коммитмента измениться позже" },
    { t: "sha256(commitment)", note: "хеш KZG-коммитмента, связывающего блоб" },
    { t: "[1:]", note: "отбросить его первый байт; байт версии занимает это место" },
  ]}
  caption="EVM видит только этот 32-байтовый версионированный хеш — байты блоба никогда не попадают в слой исполнения."
/>

<Callout type="warning" title="Что ломается">
Блоб опубликован и доказуем — но это всё ещё 128 КБ, а блок может нести несколько. Хранить всё это на каждом узле вечно — это ровно та стоимость, которую мы и собирались убить. Смысл никогда не был в том, чтобы его *хранить*.
</Callout>

**→ Шаг 3: выбрасываем данные (намеренно).**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Выбрасываем потом">

## Доступность, а не хранение: отсечение через ~18 дней

Вот прыжок, который делает это дешёвым. Узлы хранят сырые байты блоба только в течение **окна — около 18 дней** (4096 эпох) — затем **удаляют их**. Этого окна достаточно с запасом: достаточно долго, чтобы любой роллап, оспаривающий или пользователь скачали данные, и чтобы любое доказательство мошенничества или валидности успело разрешиться. После того как оно проходит, данные выполнили свою единственную задачу. Цепочка хранит крошечный **коммитмент** в истории вечно, но не 128 КБ за ним.

<Scene name="blob-prune" lang="ru" caption="Данные блоба хранятся ~18 дней — достаточно долго, чтобы скачать и доказать, — затем отсекаются. В истории остаётся только 48-байтовый коммитмент. Доступность, а не постоянное хранение." />

В этом весь концептуальный сдвиг: роллапам никогда не было нужно *постоянное хранение*, им была нужна *временная доступность*. Разделение этих двух вещей превращает «дорого навсегда» в «дёшево и одноразово».

<Callout type="warning" title="Что ломается">
Одна вещь всё ещё протекает. Если за блобы платят тем же газом, что и за исполнение, всплеск данных роллапа подскочит комиссию *вашего* свопа, а загруженный L1 подскочит стоимость блобов — рынки всё равно будут перетекать друг в друга. Блобам нужна собственная цена.
</Callout>

**→ Шаг 4: оцениваем их отдельно.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-4" num="4" badge="шаг" title="Собственный рынок комиссий">

## Отдельный рынок комиссий: blob-газ

Оцениваем блобы в собственной единице — **blob-газ** — с собственной [базовой комиссией в стиле EIP-1559](/eip-1559-fee-market), полностью отдельной от исполнения. Каждый блок **целится в 3 блоба** и допускает до **6**; базовая комиссия блобов двигается вверх или вниз за блок к этой цели и **сжигается**, точно как базовая комиссия исполнения. Поток данных роллапа теперь повышает *blob*-базовую комиссию, не трогая цену газа вашего свопа, — а загруженный слой исполнения не делает блобы дороже. Два независимых сигнала перегрузки, две независимые цены.

<Scene name="blob-fee" lang="ru" caption="Блобы измеряются в собственной единице с собственной базовой комиссией — цель 3, максимум 6 за блок, сжигается, — так что спрос на блобы и спрос на исполнение никогда не давят на цену друг друга." />

<MathViz
  lang="ru"
  name="blob-exponential"
  caption="Базовая комиссия блобов в зависимости от накопленного избытка blob-газа. На уровне цели в 3 блоба или ниже избыток остаётся на месте, и цена держится около минимума; как только блоки превышают цель, избыток накапливается, и комиссия растёт экспоненциально (e^(избыток/UPDATE_FRACTION)) — по собственной кривой, никогда не касаясь цены execution-газа."
/>

<Callout type="insight" title="И это EIP-4844">
Роллапы обязаны публиковать данные → calldata была постоянной и оценивалась на общем рынке → блобы дают отдельную, невидимую для EVM полосу → KZG-коммитмент связывает каждый блоб, так что его можно верифицировать без хранения → отсечение через ~18 дней (доступность, а не хранение) → отдельный рынок blob-газа, так что он никогда не конкурирует с исполнением. Каждое исправление вынуждено недостатком предыдущего, а суммарный эффект был драматичным: комиссии L2 упали в **10–100 раз** за одну ночь. Приставка «proto» в proto-danksharding — это подсказка: это леса для полного **danksharding**: намного больше блобов, с *выборкой доступности данных (data availability sampling)*, чтобы узлы могли подтвердить публикацию блоба, проверив несколько случайных кусков, а не скачивая всё целиком.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-4844: Shard Blob Transactions", note: "Спецификация — блобы, коммитменты, рынок blob-газа", href: "https://eips.ethereum.org/EIPS/eip-4844" },
    { kind: "post", label: "Vitalik — proto-danksharding FAQ", note: "Самое ясное глубокое объяснение", href: "https://notes.ethereum.org/@vbuterin/proto_danksharding_faq" },
    { kind: "post", label: "EIP-1559, derived", note: "Контроллер базовой комиссии, который переиспользует blob-газ", href: "/eip-1559-fee-market" },
    { kind: "post", label: "You could have invented Ethereum — state", note: "Почему хранение данных вечно — это стоимость, которую нужно победить", href: "/you-could-have-invented-ethereum#step-9" },
    { kind: "tool", label: "l2fees.info", note: "Посмотрите, что блобы сделали с комиссиями L2", href: "https://l2fees.info/" },
  ]}
/>

</DepthLayer>
