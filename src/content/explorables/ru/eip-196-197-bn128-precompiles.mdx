---
title: "EIP-196/197: доказательства с нулевым разглашением on-chain"
dek: "Верификация zk-SNARK означает вычисления спаривания на эллиптических кривых — тысячи операций над полем, которые, будучи записаны как EVM-байткод, стоили бы больше газа, чем вмещает целый блок. EIP-196 и 197 добавляют эту математику как нативные прекомпайлы, так что контракт может проверить доказательство за несколько дешёвых вызовов. Это тихий фундамент приватности on-chain и каждого validity-роллапа."
summary: "Пошаговая деривация EIP-196 и EIP-197 (прекомпайлов эллиптической кривой alt_bn128), выпущенных в форке Byzantium (октябрь 2017). Почему верификация zk-SNARK on-chain требует сложения на эллиптической кривой, скалярного умножения и проверки спаривания, которые как EVM-байткод стоят астрономически дорого — намного больше лимита газа блока; и как прекомпайлы решают это: протокол предоставляет нативные реализации этих операций на кривой alt_bn128 по зарезервированным адресам (0x06 ecAdd, 0x07 ecMul, 0x08 ecPairing), вызываемые как контракт, но по цене их реальной, низкой стоимости — так что контракт-верификатор может проверить SNARK за горстку вызовов, что положило начало приватности on-chain (миксерам вроде Tornado Cash) и validity/zk-роллапам (верификация одного доказательства для целой пачки L2-транзакций)."
topic: EIPs
order: 25
spine: true
prerequisites: ["контракты и EVM", "прекомпайлы (идея)"]
updated: 2026-06-24
readingMinutes: 10
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**Byzantium, октябрь 2017.** [Глава про REVERT](/eip-140-revert) — это изменение того форка, которое вы ощущаете каждый день; а это изменение переформатировало *будущее* Ethereum. Звучит узко — арифметика на эллиптической кривой на пару опкодов, — но именно оно впервые сделало **верификацию доказательства с нулевым разглашением on-chain** доступной по цене, и из этой единственной возможности выросли приватные транзакции и каждый validity- (zk-) роллап. Проблема, которую оно решает, — жестокая стена стоимости, а инструмент, которым оно пользуется, — **прекомпайл** — один из важнейших люков аварийного выхода Ethereum. Давайте выведем его.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Математика слишком дорога" },
  { id: "step-2", label: "Реализуем нативно" },
  { id: "step-3", label: "Доказательства on-chain" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Математика слишком дорога">

## Проблема: спаривания не помещаются в блок

<KeyTerm term="zk-SNARK" def="Доказательство с нулевым разглашением того, что утверждение истинно (например, «эта пачка транзакций валидна»), которое крошечное и быстро проверяется, не раскрывая ничего сверх этого. Для его верификации нужны операции на эллиптической кривой — в частности, проверка спаривания." /> прекрасен тем, что его *проверка мала и быстра*, — но «быстро» тут относительно операции, которая для этого нужна: **спаривания на эллиптической кривой**, плюс сложений на кривой и скалярных умножений. Это тяжёлые вычисления теории чисел над большими полями. Записанная как обычный EVM-байткод — цикл по арифметике над полем опкод за опкодом, — одна-единственная верификация обошлась бы в **сотни миллионов газа**, намного больше лимита газа целого блока. Верификация доказательства on-chain просто не помещается.

<Scene name="zk-cost" lang="ru" caption="Верификация zk-доказательства требует спариваний на эллиптической кривой — тысяч операций над полем. Реализованная как EVM-байткод, одна верификация стоит сотни миллионов газа, далеко превышая лимит газа всего блока. Так что проверка доказательства on-chain — нежизнеспособная затея." />

<Callout type="warning" title="Что ломается">
EVM универсальна, но медленна; некоторые вычисления просто слишком дороги, чтобы выразить их байткодом при любой разумной цене газа. Нужен способ выполнять конкретную, интенсивно используемую криптографию по её реальной (намного более низкой) цене — не заставляя саму EVM понимать эллиптические кривые.
</Callout>

**→ Шаг 2: пусть протокол выполнит сложную часть.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Реализуем нативно">

## Прекомпайлы: нативная криптография по зарезервированным адресам

Люк аварийного выхода — это <KeyTerm term="precompile" def="Встроенная операция, доступная по зарезервированному низкому адресу (0x01, 0x02, …). Она вызывается точно как контракт, но вместо выполнения EVM-байткода клиент запускает оптимизированную нативную реализацию, оцениваемую фиксированной стоимостью газа, отражающей её реальную работу." /> (прекомпайл). Вместо того чтобы проталкивать спаривание через EVM, протокол предоставляет оптимизированную нативную (C/Go) реализацию по **зарезервированному адресу**, вызываемую как любой контракт. EIP-196 добавляет сложение и скалярное умножение на <KeyTerm term="alt_bn128 curve" def="Эллиптическая кривая (также называемая BN254), операции которой Byzantium сделал доступными: ecAdd по адресу 0x06, ecMul по адресу 0x07 и проверка ecPairing по адресу 0x08. Выбрана потому, что эффективные системы SNARK ориентированы на неё." /> (`ecAdd` по `0x06`, `ecMul` по `0x07`), а EIP-197 добавляет решающую **проверку спаривания** (`ecPairing` по `0x08`). Каждая операция тарифицируется фиксированной, скромной стоимостью газа, соответствующей тому, что операция реально стоит узлу, — а не тому, во что она обошлась бы как байткод.

<Scene name="zk-precompile" lang="ru" caption="Контракт-верификатор делает CALL к прекомпайлу (например, 0x08 для спаривания alt_bn128) точно так же, как вызов контракта, но клиент выполняет быструю нативную реализацию и списывает фиксированную низкую стоимость газа. EIP-196 добавляет ecAdd/ecMul (0x06/0x07); EIP-197 добавляет проверку ecPairing (0x08)." />

<SpecExcerpt
  lang="ru"
  source="EIP-197: Precompiled contracts for optimal ate pairing check on bn128"
  href="https://eips.ethereum.org/EIPS/eip-197"
  ref="прекомпайл спаривания"
>
Добавляет прекомпайл по адресу `0x08`, выполняющий проверку оптимального ate-спаривания на
кривой alt_bn128: получив пары точек, он возвращает 1, если произведение спариваний равно
единице, иначе 0. Вместе с прекомпайлами `0x06` (сложение) и `0x07` (скалярное умножение) из
EIP-196 это делает верификацию zk-SNARK возможной в пределах лимита газа блока.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Ничего не ломается — мы просто сделали невозможное вычисление доступным по цене. Интересная часть — то, что контракт теперь может *делать*, раз способен проверить доказательство за несколько тысяч газа.
</Callout>

**→ Шаг 3: проверять что угодно, не раскрывая ничего.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Доказательства on-chain">

## Выигрыш: приватность и validity-роллапы

Раз проверка спаривания стала доступной по цене, контракт может **проверить SNARK за горстку вызовов** — а SNARK способен удостоверить почти что угодно, не раскрывая ничего сверх «это правда». Отсюда напрямую выросли два огромных применения. **Приватность**: контракт-миксер может принять доказательство «я владею депозитом в этом множестве и не выводил его», не раскрывая, *каким* именно депозитом, — это позволяет делать приватные переводы (схема, лежащая в основе Tornado Cash и решений в духе Zcash). И **масштабирование**: <KeyTerm term="validity rollup" def="Layer-2, который выполняет транзакции офчейн и публикует в L1 одно сжатое доказательство того, что вся пачка была валидна. Контракт в L1 проверяет это доказательство — дёшево, благодаря прекомпайлу спаривания, — вместо повторного выполнения каждой транзакции." /> (zk-роллап) может доказать, что вся пачка из тысяч L2-транзакций была выполнена корректно, а контракт в L1 проверяет это *одно* доказательство вместо повторного прогона пачки.

<Scene name="zk-unlock" lang="ru" caption="Как только контракт может дёшево проверить SNARK, он может принять доказательство того, что нечто истинно, не узнав ничего сверх этого, — это движет приватными транзакциями (доказать валидность, скрыть детали) и validity-роллапами (одно доказательство заменяет целую пачку L2-транзакций)." />

<Callout type="insight" title="Вот что такое EIP-196/197">
Верификация zk-SNARK требует **спариваний** на эллиптической кривой, которые как EVM-байткод стоят больше газа, чем вмещает блок → так что реализуем эту математику **нативно**: прекомпайлы по зарезервированным адресам (`0x06` ecAdd, `0x07` ecMul, `0x08` ecPairing на alt_bn128), вызываемые как контракты, но по их реальной, низкой цене → теперь контракт проверяет доказательство за несколько дешёвых вызовов. Эта единственная возможность — **дешёвая верификация доказательств on-chain** — семя приватности on-chain и каждого **validity- (zk-) роллапа**, а значит, несущая опора всей дорожной карты масштабирования Ethereum. Пара EIP вида «просто добавим немного математики кривых» оказалась одной из самых значимых вещей, которые когда-либо приносил Byzantium. (Более новые кривые и прекомпайлы, вроде [BLS12-381 из EIP-2537](https://eips.ethereum.org/EIPS/eip-2537), продолжают этот паттерн.)
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper
  lang="ru"
  links={[
    { kind: "eip", label: "EIP-197: bn128 pairing precompile", note: "Спецификация — проверка спаривания 0x08", href: "https://eips.ethereum.org/EIPS/eip-197" },
    { kind: "eip", label: "EIP-196: bn128 add & mul", note: "Прекомпайлы кривой 0x06 / 0x07", href: "https://eips.ethereum.org/EIPS/eip-196" },
    { kind: "post", label: "Глава 2 — EVM", note: "Почему некоторая математика слишком дорога как байткод", href: "/the-evm" },
    { kind: "post", label: "EIP-4844, выведенный", note: "Блобовая полоса, масштабировавшая роллапы, которые это включило", href: "/eip-4844-blobs" },
    { kind: "tool", label: "evm.codes — precompiles", note: "Таблица прекомпайлов по зарезервированным адресам", href: "https://www.evm.codes/precompiled" },
  ]}
/>

</DepthLayer>
