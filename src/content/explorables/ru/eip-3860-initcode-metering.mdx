---
title: "EIP-3860: замер кода, который производит код"
dek: "Байткод рантайма контракта ограничен по размеру с 2016 года — но initcode, который его производит, не был ни ограничен, ни оплачен за O(n)-работу, которую каждый узел выполняет при его обработке. Подсуньте мегабайты initcode — и вся сеть просканирует их почти бесплатно. EIP-3860 ограничивает initcode и берёт плату за слово."
summary: "Пошаговая деривация EIP-3860 (ограничение и замер initcode), выпущенного в Shanghai (апрель 2023). Почему в создании контрактов была дыра для отказа в обслуживании — байткод рантайма ограничен 24576 байтами (EIP-170) и оплачивается при депонировании, но initcode, который его возвращает, был не ограничен, а его побайтовая обработка (в первую очередь анализ JUMPDEST, который каждый узел выполняет над всем initcode) не была замерена, так что атакующий мог подать огромный initcode, заставляя всю сеть выполнять O(n)-работу далеко ниже её реальной стоимости; и как EIP-3860 закрывает эту дыру, взимая 2 газа за каждое 32-байтовое слово initcode и ограничивая initcode 49152 байтами (вдвое больше лимита рантайма), так что стоимость отслеживает работу, а размер ограничен; итог — безопасное от DoS создание контрактов, согласованная пара лимитов вместе с рантайм-лимитом EIP-170 и небольшая, но реальная надбавка к газу для фабричных паттернов CREATE/CREATE2."
topic: EIPs
order: 34
spine: true
prerequisites: ["создание контрактов (CREATE / CREATE2)", "газ и замер работы сети"]
updated: 2026-07-02
readingMinutes: 7
---

import DepthLayer from "@/components/explorable/DepthLayer.astro";
import Scene from "@/components/explorable/Scene.astro";
import Callout from "@/components/explorable/Callout.astro";
import KeyTerm from "@/components/explorable/KeyTerm.astro";
import GoDeeper from "@/components/explorable/GoDeeper.astro";
import JourneyNav from "@/components/explorable/JourneyNav.tsx";
import SpecExcerpt from "@/components/explorable/SpecExcerpt.astro";

**Shanghai, апрель 2023 года.** Развёртывание контракта — это действие из двух частей. Вы отправляете небольшую программу — **initcode**, — и EVM её исполняет; какие бы байты она ни *вернула*, они становятся постоянным **кодом рантайма** контракта. С 2016 года код рантайма ограничен 24576 байтами (EIP-170), и вы платите за его хранение. Но сам initcode проскользнул мимо: он никогда не был ограничен по размеру, а O(n)-работа, которую сеть обязана выполнить лишь для того, чтобы *обработать* его, не была полностью оплачена. Это небольшая трещина — а в газовой экономике неоценённая O(n)-операция — это ровно форма отказа в обслуживании. **EIP-3860** её запечатывает. Давайте его выведем.

<JourneyNav client:load steps={[
  { id: "step-1", label: "Неоценённое сканирование" },
  { id: "step-2", label: "Замер и ограничение" },
  { id: "step-3", label: "Два согласованных лимита" },
]} />

<DepthLayer lang="ru" tier="mechanism" id="step-1" num="1" badge="шаг" title="Неоценённое сканирование">

## Проблема: initcode был не ограничен, а его O(n)-работа недооценена

Когда EVM получает initcode, он не просто его исполняет — сначала он обязан проделать работу, пропорциональную *длине* initcode, независимо от того, что делает код. Самый наглядный пример — <KeyTerm term="анализ JUMPDEST (JUMPDEST analysis)" def="Перед исполнением байткода EVM сканирует всю программу, чтобы отметить, какие байтовые смещения являются валидными точками перехода (опкоды JUMPDEST). Это O(n) по длине кода, и его нужно выполнить над всем initcode — работу, которую повторяет каждый узел, независимо от того, доработает ли код до конца." />: каждый узел сканирует весь initcode, чтобы найти валидные цели перехода. Код рантайма был ограничен и оплачивался, а initcode был **не ограничен**, и это побайтовое сканирование не было замерено пропорционально его размеру. Так что атакующий мог подсунуть сети **мегабайты** initcode и заставить каждый узел перемалывать их за плату далеко ниже реальной стоимости — недооценённая O(n)-операция, классическая форма DoS.

<Scene name="ic-gap" lang="ru" caption="Создание контракта исполняет initcode, чтобы вернуть код рантайма (ограниченный 24576 байтами со времён EIP-170). Но сам initcode был не ограничен, а O(n)-сканирование JUMPDEST, которое каждый узел выполняет над ним, было недооценено — так что огромный initcode заставляет всю сеть работать почти бесплатно. Недооценённый отказ в обслуживании." />

<Callout type="warning" title="Что ломается">
Сторона рантайма застёгнута: ограниченный размер, оплаченный депозит. Сторона *создания* — нет. Работа, масштабирующаяся с длиной initcode, навязывается каждому узлу, но не оплачивается пропорционально — а у initcode вообще нет потолка размера. Нужно чинить и то, и другое.
</Callout>

**→ Шаг 2: берём плату за размер и ограничиваем его.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-2" num="2" badge="шаг" title="Замер и ограничение">

## EIP-3860: два газа за слово плюс жёсткий потолок

Решение — тот же принцип, который Ethereum применяет всякий раз, когда находит неоценённую O(n)-работу: сделать стоимость отслеживающей размер. EIP-3860 добавляет <KeyTerm term="плату за слово initcode (initcode word charge)" def="Дополнительные 2 газа за каждое 32-байтовое слово initcode, взимаемые с CREATE, CREATE2 и транзакций создания контрактов. Она напрямую оценивает работу, пропорциональную длине (такую как анализ JUMPDEST), которую сеть выполняет над initcode." /> в **2 газа за 32-байтовое слово** initcode на `CREATE`, `CREATE2` и транзакции создания — так что чем длиннее initcode, тем больше вы платите, ровно соответствуя сканированию. И добавляет <KeyTerm term="ограничение размера initcode (initcode size cap)" def="Жёсткий лимит в 49152 байта (MAX_INITCODE_SIZE = 2 × MAX_CODE_SIZE) на initcode. Превышение немедленно приводит к отказу, ограничивая наихудшую работу, которую может навязать одно создание контракта." /> в **49152 байта** — ровно вдвое больше лимита рантайма, — так что ни одно создание не может быть неограниченно большим. Теперь стоимость масштабируется вместе с работой, а работа ограничена.

<Scene name="ic-meter" lang="ru" caption="EIP-3860 взимает 2 газа за каждое 32-байтовое слово initcode, так что стоимость отслеживает сканирование, пропорциональное длине, и ограничивает initcode 49152 байтами (2× лимита рантайма). Никакого неограниченного initcode, никакой неоценённой O(n)-работы — стандартное решение «замерить работу»." />

<SpecExcerpt lang="ru"
  source="EIP-3860: Limit and meter initcode"
  href="https://eips.ethereum.org/EIPS/eip-3860"
  ref="плата"
>
Вводит максимальный размер initcode в 49152 (2 × MAX_CODE_SIZE) и взимает дополнительные
2 газа за каждое 32-байтовое слово initcode, применяемые к CREATE, CREATE2 и транзакциям
создания, — оценивая стоимость анализа jumpdest и хеширования над initcode.
</SpecExcerpt>

<Callout type="warning" title="Что ломается">
Для почти любого настоящего контракта — ничего: плата за слово крошечная, а 49152 байта — щедрый лимит. Меняется худший случай: атакующий больше не может вооружиться гигантским initcode, а фабрики CREATE2 теперь платят небольшую, пропорциональную размеру плату, которой раньше не было.
</Callout>

**→ Шаг 3: создание и рантайм наконец совпадают.**

</DepthLayer>

<DepthLayer lang="ru" tier="mechanism" id="step-3" num="3" badge="шаг" title="Два согласованных лимита">

## Итог: симметричное, безопасное от DoS создание

С EIP-3860 две половины жизни контракта наконец симметричны: **initcode** ограничен 49152 байтами и замерен по слову; **код рантайма** ограничен 24576 байтами ([EIP-170](https://eips.ethereum.org/EIPS/eip-170)) и оплачивается при депонировании. Создание контракта безопасно от DoS — никакого неограниченного ввода, никакой неоценённой O(n)-работы. Изменение почти незаметно для обычных развёртываний и слегка повышает стоимость паттернов <KeyTerm term="фабрика CREATE2 (CREATE2 factory)" def="Контракт, разворачивающий другие контракты через CREATE2, часто по детерминированным адресам. Поскольку такие паттерны разворачивают множество контрактов, новая плата за слово initcode заметнее всего именно здесь." />, разворачивающих контракты массово. Это небольшой EIP с ясной родословной: тот же урок, что и [EIP-150](/eip-150-io-repricing) — когда сеть выполняет работу, пропорциональную вашему вводу, вы обязаны платить пропорционально.

<Scene name="ic-result" lang="ru" caption="Initcode (≤ 49152 байт, +2 газа/слово) и код рантайма (≤ 24576 байт, EIP-170) теперь согласованная, замеренная пара. Создание безопасно от DoS; фабрики CREATE2 платят небольшую плату за слово. Тот же принцип «замерь O(n)-работу», что и в переоценке I/O из EIP-150." />

<Callout type="insight" title="И это и есть EIP-3860">
Код рантайма был ограничен и оплачен, а **initcode**, который его производит, был не ограничен, и его пропорциональная длине обработка (анализ JUMPDEST) была недооценена → атакующий мог подать огромный initcode и навязать **O(n)-работу каждому узлу** почти бесплатно — недооценённый DoS → EIP-3860 взимает **2 газа за 32-байтовое слово** initcode и ограничивает его **49152 байтами** → так что стоимость отслеживает работу, размер ограничен, создание безопасно от DoS, и всё это зеркалит рантайм-лимит EIP-170. Это на вид незначительная правка газа, закрывающая реальную поверхность атаки, и учебниковое применение самого старого урока замера в Ethereum: оценивайте работу, которую сеть действительно выполняет.
</Callout>

</DepthLayer>

<DepthLayer lang="ru" tier="deeper" id="refs">

## Погружаемся глубже

<GoDeeper lang="ru"
  links={[
    { kind: "eip", label: "EIP-3860: Limit and meter initcode", note: "Спецификация — потолок + плата за слово", href: "https://eips.ethereum.org/EIPS/eip-3860" },
    { kind: "eip", label: "EIP-170: Contract code size limit", note: "Лимит рантайма в 24576 байт, который это зеркалит", href: "https://eips.ethereum.org/EIPS/eip-170" },
    { kind: "post", label: "EIP-150 — I/O repricing", note: "Изначальный урок «оценивай реальную работу»", href: "/eip-150-io-repricing" },
    { kind: "post", label: "EIP-1014 — CREATE2", note: "Фабрики с детерминированными адресами, которые это оценивает", href: "/eip-1014-create2" },
  ]}
/>

</DepthLayer>
